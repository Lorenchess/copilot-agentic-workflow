# Phase 3 — Acceptance-Test Quality, RED Proof & Test-Contract Integrity
## Contract (Fable → Sonnet 5 handoff)

Status: **revision 2, approved for the P3-C1 implementation handoff by the owner on 2026-09-10** (architecture review `docs/reviews/2026-09-10-phase-3-architecture-review.md`: PROCEED_WITH_CHANGES, M1–M5; owner decisions D1–D4 ACCEPT_WITH_CHANGES, D5 ACCEPT; draft-contract review `docs/reviews/2026-09-10-phase-3-contract-review.md`: C1–C3 and the file-scope note, applied; D6 decided (a); revision-2 review `docs/reviews/2026-09-10-phase-3-contract-revision-2-review.md`: PROCEED). Push and lock remain separately authorized. Supersedes the proposal `docs/specs/2026-09-10-phase-3-test-contract-integrity-architecture-proposal.md`, kept unedited as the reviewed record. The Phase 1 reference (`03d4230`, `phase-1-reference`) and the Phase 2 reference (`5a46eb7`, `phase-2-reference`) are not reopened; everything here is forward evolution of current `main` (`c3904cf`).

Roles: Fable is architect, planner, and independent reviewer; Sonnet 5 (`model: "sonnet"`, verified `claude-sonnet-5`) implements each checkpoint; at most two correction rounds per checkpoint; Fable commits on PASS; Astra reviews the result; push and lock need owner approval.

## 1. Question, boundary, and binding constraints

**Question.** How does Tester convert the approved Phase 2 plan into an executable definition of correctness that genuinely protects the Developer/Verifier workflow — and how do the test contract, the review that accepts it, and the later verification claim keep describing the same approved behavior and the evidence actually available?

**Boundary.** Phase 3 changes Tester, TEST-CONTRACT.md, RED-REPORT.md, test-change semantics, and adds one bounded Adversary test review with its own artifact. It touches Planner not at all, and Developer, Verifier, `pr`, and `pipeline` only at the interfaces that give the contract meaning through progression, resume, verification, and publication (T10–T13). Implementation strategy, GREEN iteration, the Verifier's broader semantic review, delivery integration, new test infrastructure, and model benchmarking stay deferred.

**Constraints** (Phase 1 §2 and Phase 2 §1 continue): Copilot-native Markdown assets only; no runner, harness, schema, scheduler, database, lock, or state machine; nine roles; twelve artifacts after this phase (TEST-REVIEW.md added); four gates; every existing STOP code and message unchanged; every agent's `tools:` list byte-identical to `5a46eb7`; Sonnet-5 for every role. Phase 1 already requires business-outcome assertions and rejects compile/setup failures as RED (`tester.agent.md` steps 4–7): Phase 3 makes those rules operational — which production path stays real, where the assertion is, whether discovery happened, and a second reader on the source — it does not claim to introduce them.

## 2. Review disposition

| Finding | Disposition | Where resolved |
|---|---|---|
| M1 — proof boundary permits false persistence and interface confidence | Accepted. Boundary chosen by required outcome; fake persistence allowed only for local decision criteria; changed wire contracts need adapter/decoding evidence or a recorded gap; field lists are design checks; input stubs allowed. | T3, T4, T8 |
| M2 — substitute acceptance has no downstream meaning | Accepted. Equivalent vs partial evidence; `NOT_VERIFIED` clause; Verifier verdict `INCOMPLETE`; no G4 on `INCOMPLETE` (D6 decided (a)). | T10, T11 |
| M3 — `NARROWS` lowers the approved requirement | Accepted. Effect measured against the PLAN clause; `REQUIREMENT_CHANGE` is never amendable; every amendment re-reviewed for discrimination. | T12 |
| M4 — review acceptance and anchors not tied to evidence | Accepted. TEST-REVIEW.md carries a basis; dependency-set re-review; anchor transition recorded and reviewed; stage-5b entry defined; corrections supersede. | T11, T12 |
| M5 — reused tests and pre-existing support bypass integrity | Accepted. Relied-on tests need an observed baseline and observed execution at verification; proof-relevant envelope files change only through the amendment path. | T5, T9, T12 |
| Overstatement of W2/W5; timeout wording; inconsistent-run diagnosis; scaffold prerequisite; adversary independence; example source excerpts; depth by risk | Accepted. | §1, T6, T7, T11, T13, §6 |
| C1 — a stat diff cannot establish the approved delta | Accepted. Transition evidence is a content patch over every controlled path between the prior anchor and the candidate; the reviewer compares the patch with the approval before the candidate becomes the anchor. | T12, T11 |
| C2 — gap, exception, and amended-PASS prerequisites conflict | Accepted. One evidence-package definition; recovery returns to the earliest missing evidence (stage 5, then 5b, then 6 if needed, then 7); authorized exceptions are RUN.md decisions the Verifier recognises; post-implementation results are recorded honestly and always re-reviewed. | T10, T11, T12, §4 |
| C3 — configuration exempted from proof relevance by type; actor mismatch | Accepted. Proof relevance follows effect for every envelope file; the example classifies its build file by inspection; Tester is the single writer of any approved amendment delta on a controlled path. | T9, T12, §6 |
| File-scope contradiction (`pipeline` skill listed as both modified and unchanged) | Accepted; exception stated. | §5, §6 |

## 3. Design rules

Each rule is normative; T-numbers are referenced by the checkpoints. "Rows" and "tables" are Markdown; ids are stable strings kept across revisions.

### T1 — Inputs and the use of PLAN.md

Tester reads `.github/skills/test-contract/SKILL.md` explicitly at the start of its procedure, then PLAN.md (immutable, its only artifact input). It never reads INTAKE.md, RUN.md, ADVERSARY-REVIEW.md, or IMPLEMENTATION.md except the single `TEST-CHANGE-REQUEST` entry named on an amendment or assessment invocation.

PLAN.md is consumed, never re-derived: `PRESERVED` items → `P` rows → PRESERVATION; `IMPLEMENTED` items → AC ids; `EXCLUDED`/`UNRESOLVED` items are never tested; change class → depth (T14); `RESPONSIBLE_COMPONENT` → the code that stays real (T3); `ADJACENT_TEST` → placement, reuse, relied-on tests, obsolete-test prediction (T5, T12); `NOT_FOUND` → the expected reason a WITNESS is RED (T6); `INTERFACE`/`CROSS_REPO_CALL`/`I` rows → the interface evidence (T8); Affected files `EDIT` entries → the modified boundary (T5); Observed at → the required observable outcome and its boundary (T3); Preconditions → the Given; Path `NEGATIVE` → its own test; every combined-outcome statement → its own test; `ACCEPTED` risks naming test conditions → determinism controls (T7); `ASSUMED` rows → never tested as criteria. A Tester finding that the plan is wrong (an absent route, an interface disagreement, a missing clause, more required behavior discovered) is routed through the existing STOPs or recorded as a gap for planning — never fixed in the contract.

### T2 — Contract map and Proves lines

TEST-CONTRACT.md's **Contract map** has one row per test (or per uncovered clause): Test (`repository: path#identity`) · Criterion (AC/P id and clause when the test covers one clause) · Level with a one-line reason tied to Observed at (T3) · Observes (the outcome asserted, in business terms) · Doubles (`none` or `boundary → kind`, T4; a shared declaration may be referenced instead of repeated per row) · Classification with basis (T5) · Expected before implementation (`RED` / `PASS`) · Determinism (`none` or the controls, T7). Each test has one **Proves** sentence: "fails when `<outcome>` is absent because `<assertion in words>`; passes only when `<the Then>` holds."

Every AC and `P` row in PLAN.md appears at least once. A clause with no test appears with Test `—` and one of `TEST_BOUNDARY_MISSING (raised)`, `NOT_VERIFIED (environment: <condition>; partial evidence: <test id> or none)` (T10), or `COVERED BY <test id>`. A criterion whose behavior partially exists is split at clause level: existing clauses PRESERVATION, missing clauses WITNESS. Where clauses relate (delays computed and retries performed), one test observes the combined behavior (retries actually scheduled at those delays); unrelated helper tests do not establish the combination.

### T3 — Proof boundary and level selection (M1)

**Rule.** The boundary is chosen by the outcome the approved clause requires, never by repository membership or double vocabulary. The test observes the *Then* at the route Observed at names, or at a lower route that shows the identical state, with the code that produces the outcome real. Levels: `UNIT` · `SERVICE` · `API` · `UI` · `E2E`; choose the lowest that is **sufficient**.

| Required outcome | Sufficient evidence | Not sufficient |
|---|---|---|
| Local decision, calculation, state transition, whose criterion is the decision itself | `UNIT`/`SERVICE`; a state-bearing fake repository may hold the state | — |
| Persisted outcome (a row, column, new field, new enum value, migration-dependent read) | The real repository/mapping code against the storage the repository's tests already use (`EMBEDDED`), or the real store when only it exhibits the behavior | A fake repository: it proves the intent to save, not the mapping, transaction, or stored column |
| Response contract: validation error body naming the field, status mapping, serialization, filters, paging | `API` through the real route with the framework's test client/embedded server | `UNIT` of the validator alone |
| Emitted call with business content | `SERVICE` with a recorder at the boundary, arguments asserted | Invocation verified without arguments |
| Changed wire contract between repositories | T8's interface evidence | Domain-object recorder alone |
| UI behavior | `UI` component test | `E2E` unless the journey crosses systems |
| Cross-system journey whose Then neither side's route shows | `E2E` only when T8 cannot prove it and the environment can run it; otherwise T10 | — |

Never choose a level because it is easier to make RED, and never choose `E2E` to avoid deciding a boundary. The level and its reason are recorded per row; the review checks the reason against Observed at.

### T4 — Doubles (M1, narrowed)

**Principle — real inside, doubled at the edge, doubles observe or supply a Given.** The `RESPONSIBLE_COMPONENT` and every collaborator that participates in producing the required outcome are real. A boundary may be doubled where it crosses a process or I/O edge — another repository or service, a store (only when persistence is not the required outcome, T3), a broker, the clock, randomness, the filesystem, the network. A double exists to **observe** (record arguments, hold state the test asserts) or to **supply an input the Given requires** (the ledger call fails once; a lookup returns a fixture).

Vocabulary (Doubles column): `REAL` · `EMBEDDED` · `FAKE` (state-bearing; state asserted) · `RECORDER` (calls and arguments asserted) · `STUB` (canned input for a Given).

Forbidden: (1) doubling the responsible component, the method under test, or a collaborator whose behavior is part of the criterion; (2) a double that substitutes for the behavior being proved, or that supplies the asserted result without the required transformation having run (ordinary input stubs are allowed even when they influence the result); (3) verifying a call without asserting the arguments that carry the business content when the criterion names that content; (4) doubling persistence when persistence is the required outcome; (5) shaping a cross-repository double from implementation convenience rather than from the `I` row and T8. When applying the principle would leave the responsible path doubled, the level is wrong or the case is `TEST_BOUNDARY_MISSING` — never a bigger mock. Doubles are tester-created support files (T9) or the repository's existing doubles cited by `ADJACENT_TEST`; never a new dependency (T13).

### T5 — Classification, preservation bases, relied-on existing tests (M5)

Classification is per test and per clause (T2). WITNESS/PRESERVATION semantics, the bounded-correction rule, `TESTS_NOT_RED`, and the `PRESERVATION_BASELINE_FAILED` diagnosis (test defect / baseline failure / environment) are unchanged. The developer decision "behavior already exists" at `TESTS_NOT_RED` is recorded as `PRESERVATION — ALREADY_SATISFIED (developer decision)` with the plan's evidence gap noted, never corrected by Tester.

PRESERVATION is warranted only by, in order: (1) every `P` row (required); (2) the existing clauses of a partially existing criterion; (3) behavior on a modified boundary that the change directly threatens — named by an Affected files `EDIT` entry or a cited evidence row, recorded as the basis; "one or two per modified boundary" restrains discretionary expansion and never authorizes omitting a material invariant — more required behavior or scope uncertainty discovered here is surfaced for planning (existing STOP routes), not silently added or dropped; (4) nothing else.

**Relied-on existing tests.** Where an existing test cited by `ADJACENT_TEST` already asserts a required constraint, Tester records it under Relied-on existing tests (`repository: path#identity — covers P2`) instead of duplicating it, **runs it at stage 5** through the repository's runner scoped to that class, and records its observed baseline result (`PASS` from `[TOOL]` output). A relied-on test that is disabled, undiscovered, or failing at baseline cannot supply coverage: it is recorded as such and the clause gets its own test or a `PRESERVATION_BASELINE_FAILED` diagnosis as applicable. At stage 7 the Verifier requires observed execution of every relied-on identity — from the full-suite output or a scoped run — and treats a missing, skipped, or failing relied-on identity as FAIL. Relied-on tests are diffed since the anchor like envelope files (T9).

### T6 — RED evidence, validity, discovery, reproduction

RED-REPORT.md's **RED evidence** section records, per WITNESS: **Expected** (business result, then the literal assertion in words) · **Observed** · **Failure locus** (the assertion or expected-exception check inside the test method, quoted from `[TOOL]` output) · **Setup** (one line: how the Given was arranged and why the failure is not a wrong precondition — passing PRESERVATION tests do not establish this WITNESS's own setup) · **Why this proves the clause is unsatisfied** (one sentence tying Observed to the plan's evidence of absence) · **Validity** (`compiled/loaded at <anchor>: yes · failure at own assertion: yes · same-run PRESERVATION passed: yes · no other failure or error in the run: yes · reproduced on second run: yes`).

**Validity rules.** A WITNESS failure is RED only when all hold: (1) the test compiles/loads at the anchor — it references only symbols that exist there; a new status value, field, or route is observed through its representation (`status.name()`, the JSON field, the path), and "cannot resolve symbol" is never RED; (2) the failure occurs at the test's own assertion on the clause's outcome; (3) the Given was arranged as the Setup line states; (4) the same run's PRESERVATION tests pass; (5) no other test's failure or error appears in the run except other WITNESS rows' own assertion failures; (6) the contract command is run **twice** consecutively with identical per-test identities and results, recorded compactly (identities and results, plus output sufficient to inspect a discrepancy; the full suite is not run twice). A discrepancy between the two runs is **diagnosed with the evidence preserved** — a test defect (corrected within the existing budget), a race or instability in production code (recorded, routed through `TESTS_NOT_RED`/planning as the diagnosis warrants), or environment instability (`RUNNER_UNAVAILABLE`) — never repaired into stability by weakening the expectation. RED caused primarily by compilation unrelated to the clause, missing dependency or setup, unavailable infrastructure, a syntax error, an unrelated failing test, a broken fixture, or a runner failure is not RED.

**Discovery.** RED-REPORT.md's Command section records, per repository, the identities the output shows as discovered and executed against the Contract map; a missing identity is corrected before RED is claimed. Confirmation gains "every contract identity was discovered; results reproduced on a second run".

### T7 — Determinism

Prefer controlled clocks, seeded inputs, completion signals, isolated data, and deliberate ordering. Clock: inject or fake; assert scheduled delays and timestamps as values; a real-time wait is not evidence about production scheduling or broker timing. Randomness: seed or replace the source. Ordering: assert order only where required. Async: await a completion signal the code exposes; a fixed sleep is never the mechanism. Bounded time: an infrastructure timeout is not an assertion; failure to observe a required event within an approved interval, where the interval is the requirement, is a legitimate assertion. Concurrency: where an interleaving is material, use the repository's existing synchronization facilities; no sleep-based flakiness, no custom framework. Environment state: each test creates its own data with unique identifiers and leaves shared state as found. External dependencies: doubled at the edge or `EMBEDDED` via what the repository's tests already use. The second run (T6) is a repeatability sample, not proof that all races are absent. Controls are recorded in the Determinism column only where a condition exists.

### T8 — Interface evidence (M1)

Three kinds of evidence are kept distinct and named as such: **local business evidence** (T3), **interface-conformance evidence**, and **live cross-system evidence** (`E2E`). Neither of the first two establishes transport delivery, deployment availability, or storage-engine behavior that was not exercised.

For every `I` row, TEST-CONTRACT.md's **Interface evidence** table records: Interface (id, provider → consumer, the field list from the row, verbatim, labelled *design check*) · **Consumer adapter evidence** — the test that exercises the real consumer adapter/serialization path (the client that encodes the request) and asserts the **encoded** representation at the transport edge, using the repository's existing facility (a mock server or framework transport recorder cited by `ADJACENT_TEST`), plus the `NEGATIVE` test exercising the row's failure behavior through the adapter that translates a real transport failure into the declared exception · **Provider decoding evidence** — the test that exercises the real provider entry point with the encoded representation captured on the consumer side (or one shaped from the field list when capture is not available, labelled as such) and asserts the provider's outcome · **Conformance** — `ESTABLISHED` only when both adapter and decoding evidence exist and agree; `DESIGN_CHECK_ONLY` when only field-list agreement exists; `NOT_VERIFIED` when a side has no executable evidence here (then T10 applies to the interface clause) · Deployment-compatibility evidence — the `NEGATIVE` consumer test when the row names a required behavior, else `n/a — <reason>`. A domain-object recorder at the client interface is local business evidence for the consumer's decision, not interface evidence. A field disagreement between the two sides that traces to the plan is `TEST_BOUNDARY_MISSING` with the condition "interface fields disagree with `I<n>`" — Tester never reconciles the interface.

### T9 — Protected paths, envelope, proof-relevant envelope, relied-on tests (M5)

- **Protected paths** — every file Tester created (tests and support: fixtures, fakes, recorders, builders, fake clocks, test-profile configuration under the test tree) and every pre-existing test file adopted through an approved amendment (T12). Immutable from the anchor until an approved amendment; Developer never edits them; the Verifier's byte-identity diff covers all of them.
- **Envelope** — pre-existing files the contract depends on: build files, pre-existing test configuration, pre-existing shared fixtures and doubles. Developer may change an envelope file with a justification under IMPLEMENTATION.md's Envelope changes; the Verifier diffs since the anchor; an unjustified or test-excluding change is FAIL (unchanged). Each envelope entry is marked **proof-relevant: yes/no by effect, never by file type**: yes when the file's content — or a property, profile, or setting within it — determines what a contract test observes or which implementation it observes: provider or adapter selection, storage or profile selection, fixture business state, a double's semantics, clock units, test data, execution scope; no when the content has no such effect (ordinary dependency versions, unrelated build settings). A build file can therefore be proof-relevant, and the entry names the property that makes it so. Tester classifies at stage 5 with the reason; the review checks the classification. **A change to a proof-relevant envelope file is not an ordinary envelope change**: it goes through the amendment path (T12: request, assessment, decision, transition patch, dependency re-review) and is applied by Tester as the approved delta; the Verifier treats a proof-relevant change with no approved amendment as FAIL regardless of the Envelope changes justification. Ordinary (non-proof-relevant) envelope changes stay developer-owned and justified as today.
- **Relied-on existing tests** — T5; diffed like envelope files; observed at verification.

**Controlled paths** = Protected paths + proof-relevant envelope files + relied-on existing tests: the set every transition patch (T12) covers.

Tester may create anything the tests need under the test source tree. Tester may edit a pre-existing file in exactly one circumstance: as the approved delta of an amendment that names that file (an adopted test, or a proof-relevant envelope file) — this is the single narrowing of the locked own-files rule, and the Developer agent never edits any controlled path. Tester may not add a dependency or create code the production tree would compile against. A needed library or scaffold is `TEST_BOUNDARY_MISSING` naming the item (T13). Tester records, in TEST-CONTRACT.md, the `[TOOL]` staged-set output of the index-scope check taken immediately before each commit; the review compares it with Protected paths.

### T10 — Coverage gaps: equivalent evidence, partial evidence, `NOT_VERIFIED` (M2, D3 as qualified)

When the level that proves a clause cannot execute here (A6 environment conditions only), Tester first seeks **equivalent evidence**: a lower-level test that proves the same clause at the same observable boundary. Equivalence is *explained* in the Contract map row (why the boundary and outcome are the same), *challenged* by the test review (T11), and never decided by the developer. Where no equivalent exists, the runnable test is **partial evidence**, the clause is recorded `NOT_VERIFIED (environment: <condition>; partial evidence: <test id>)` in the Contract map and under **Coverage gaps** (clause, level required, condition, what the partial evidence does and does not show, the deferred test's design), and Tester raises `RUNNER_UNAVAILABLE` with the per-clause condition. The developer's options: fix the environment and resume stage 5 for that clause; **proceed with the gap** (useful local progress; the clause stays `NOT_VERIFIED`); change the requirement through planning (a `SEND_BACK`-class decision starting a new planning cycle); or abort. Proceeding is authorization of progress, never a finding of equivalence. The deferred test is not committed unless the repository already has an exclusion convention usable without editing build configuration.

**Authorized exceptions.** Two developer decisions create an *authorized exception*: proceed-with-gap at `RUNNER_UNAVAILABLE` (environment) and proceed at `TEST_REVIEW_REVISE_LIMIT` (proof defect). An exception is recorded by `pipeline` in RUN.md's decision log naming the clauses and, for a proof defect, the open finding ids it covers; the environment case is also in TEST-CONTRACT.md's Coverage gaps (written at stage 5, so it is inside the review's basis). The exception marks the named clauses `NOT_VERIFIED`; it never marks evidence equivalent, never reclassifies a test, and never touches unaffected evidence.

**Downstream meaning of `NOT_VERIFIED`** (the minimal interfaces): Developer implements against the whole plan, not only the runnable tests. The Verifier derives the `NOT_VERIFIED` clause set from Coverage gaps plus RUN.md's authorized exceptions. Its verdict is `PASS` only when that set is empty and every existing PASS condition holds; `INCOMPLETE` when every runnable check passes, the review-basis check (T11) holds, and the set is non-empty (recorded with the clauses and their source); `FAIL` is unchanged — a real test or implementation failure is FAIL whatever exceptions exist. On `INCOMPLETE`, `pipeline` voices `STOP [VERIFICATION_INCOMPLETE]: Verification is INCOMPLETE — required clause(s) <ids> are NOT_VERIFIED (<environment | proof defect>). Decision needed from: developer. Close the gap and resume at stage 5 for those clauses, change the requirement through planning, or abort.` **Recovery always returns to the earliest missing evidence:** stage 5 (Tester authors and commits the deferred test, or corrects the defective one, records its observed result honestly per T11's package rule), then 5b (re-review of the affected set), then 6 only if a recorded RED requires implementation, then 7 again. Resuming at stage 7 alone can never close a gap. G4 is not asked on `INCOMPLETE`; stage 9/10 eligibility continues to require `PASS`. Owner decision D6 is (a): `INCOMPLETE` never reaches G4; there is no qualified publication path in this reference.

### T11 — Test review, stage 5b (D1 as qualified; M4)

**Entry — one package rule for every review.** Stage 5b (initial review, correction round, re-review after an amendment, re-review after recovery) starts only on a **complete evidence package**: every contract test has an observed result recorded per T6's shape and validity rules; classification is unchanged unless a recorded developer decision changed it; relied-on baselines, coverage gaps, staged-set records, and (after the first anchor) the transition patch (T12) are recorded. The RED obligation is specific: **before any implementation exists** (the initial package and correction rounds), every WITNESS must be RED per T6 and every PRESERVATION PASS or diagnosed; an all-PRESERVATION contract authorized by recorded decisions is a valid package. **After implementation exists** (an amendment, a test adopted or deferred and authored later, a recovery), each test's result is recorded as observed — `RED` (then stage 6 follows the re-review) or `PASS — <amended | authored> after implementation` — never forced RED and never automatically reclassified; the re-review's discrimination check (method step 2 and the catalogue) is what stands in for the RED proof those tests cannot give. Tester never forces false RED to "return RED".

**Reader and inputs.** `adversary`, in test-review mode, read-only over tests, plan, and code; writes only TEST-REVIEW.md; no terminal (it assesses source and recorded execution evidence, not runtime success). Inputs: the `test-contract` skill (challenge section), PLAN.md, TEST-CONTRACT.md, RED-REPORT.md, the controlled paths (repository read), the staged-set records and the transition patch `[TOOL]` recorded in TEST-CONTRACT.md, and, on a re-review, the amendment entry's approved delta; cited evidence locations as needed. It does not read `challenge-plan` or its own ADVERSARY-REVIEW.md in this mode; plan approval is never evidence of test adequacy.

**Method (order is the method).** (1) From PLAN.md alone, derive per clause the required observable outcome and one or more plausible incorrect behaviors (the proxy, the doubled outcome, the wrong column, the missing combination). (2) Then inspect the test source, the Contract map, doubles, proof-relevant support, RED evidence, and staged-set record, asking for each clause: *would the incorrect behaviors pass this test?* (3) Work the challenge catalogue (skill): map completeness · assertion target vs Observed at, from source · boundary and doubles (T3/T4) · level sufficiency · RED validity and setup (T6) · clause classification and clause relationships · preservation bases and relied-on baselines · negative/boundary/combined cases from PLAN.md · determinism where a condition exists · interface evidence kind and conformance value (T8) · discovery · protected/envelope/proof-relevant split against the staged set, with proof-relevance checked by effect · coverage-gap and equivalence honesty (T10) · on re-review: the transition patch equals the approved delta and nothing else (every hunk in every controlled path accounted for; membership changes accounted for), discrimination of amended or later-authored tests, the dependency set. (4) Findings `id · category · severity · evidence · what the contract must show or change`; the reviewer identifies defects and the evidence needed, never authors tests. A zero-finding ACCEPT with checks recorded is legitimate; no count sentence.

**Severity and verdict.** `ACCEPT` — no unresolved blocking test-quality defect in the current evidence. `REVISE` — a specific correction or an existing STOP is needed. No `BLOCK`. HIGH (→ REVISE): a WITNESS whose assertion does not target the clause's outcome; the responsible path doubled; RED at a non-assertion locus or with an unarranged Given; an AC/P clause missing from the map; a required boundary or combined clause absent; interface conformance claimed beyond its evidence; a non-equivalent substitute presented as equivalent; a tester-created or proof-relevant file outside protection; a discriminating check failing on re-review. MEDIUM: fixed or residual (a required clause is never left residual while completeness is claimed — severity follows effect). LOW: note. Depth follows the contract's actual risks: a SMALL contract gets one compact pass.

**Basis and freshness.** TEST-REVIEW.md records its **Basis**: PLAN.md `cycle.round`; TEST-CONTRACT.md revision (anchor SHA per repository, correction round, latest amendment id); RED-REPORT.md revision; the coverage-gap set. RUN.md's Artifact history records TEST-REVIEW.md `ACTIVE`/`SUPERSEDED` like every artifact. Any correction, amendment, proof-relevant envelope change, coverage/limitation change, or supersession of PLAN.md marks the review `SUPERSEDED` in the same RUN.md edit; the re-review covers the **affected dependency set** (the amended tests, every test depending on changed support or configuration, and any coverage or gap changed), recorded by Tester in the amendment entry and verified by the reviewer. **Current review** means either (i) a `CURRENT ACCEPT` whose basis equals the ACTIVE revisions it names, or (ii) a `CURRENT REVISE` every one of whose open HIGH findings is covered by an authorized exception recorded in RUN.md (T10) — the proof-defect case; nothing else counts. Developer proceeds (or resumes after an amendment or recovery) only on a current review; the Verifier's review-basis check accepts exactly these two forms and, in form (ii), can render at most `INCOMPLETE`. Resume: TEST-REVIEW.md missing or SUPERSEDED → stage 5b; TEST-CONTRACT.md superseded by a correction or by recovery → stage 5; never stage 6 without a current review.

**Loop.** One initial review plus one automatic Tester correction round (stage 5 re-invocation: corrections, both runs, new commit recorded under Correction rounds with the superseded anchor and the transition patch; prior RED/review evidence SUPERSEDED). A second REVISE raises `STOP [TEST_REVIEW_REVISE_LIMIT]: The test contract still has a HIGH finding after one correction round. Decision needed from: developer. Redirect the tester (one explicit, bounded human-directed round), proceed with the affected clause(s) recorded NOT_VERIFIED (proof defect — an authorized exception, T10), or abort.` No routine human gate; ACCEPT is silent.

### T12 — Amendments: authority, effect, assessment, anchors, adoption (M3, M4, D2, D4 as qualified)

**Authority.** *An amendment may change how a test observes; it may never change what the approved clause requires.* Effect is measured against the PLAN.md clause, not against the old assertion. Human approval of a `TEST-CHANGE-REQUEST` never substitutes for planning.

**Effect enum** (assessment and record): `OBSERVATION_ONLY` (same required outcome, different route, fixture, or literal) · `SUPPORT_ONLY` (support/configuration change with the dependency set named) · `OVERCONSTRAINT_REMOVED` (the defective test demanded more than the clause; the amended test still proves the **entire** approved outcome — legitimate) · `ADOPTION` (a pre-existing test made obsolete by an approved criterion, D2) · `REQUIREMENT_CHANGE` (removes part of the approved outcome, changes success semantics, or accepts different behavior — **not amendable**: routed as a `SEND_BACK`-class decision to a new planning cycle, which supersedes the contract) · `INCOMPLETE` (request lacks required content; returned before any STOP).

**Request content** (Developer or Tester origin): test identity; the assertion or setup at issue; the PLAN.md clause and evidence; the proposed change as an observation change; an explicit statement whether the clause's required outcome is unchanged.

**Assessment (D4).** On any request, `pipeline` invokes Tester in assessment mode before voicing `TEST_CHANGE_REQUESTED`. The assessment is concise: effect; affected clauses and classification impact; required behavior unchanged/changed against PLAN.md; evidence that becomes stale (which RED, review, relied-on baselines) and what must be re-established; recommendation (apply / decline / planning). It is appended to TEST-CONTRACT.md's Amendments and voiced with the request. The Tester assessing its own test is not independent review; the re-review (below) is.

**Tester-origin route at stage 5 (D2).** When `ADJACENT_TEST` evidence shows a pre-existing test whose expectation an approved criterion contradicts, Tester records a `PRE-EXISTING TEST CONFLICT` (file, identity, the clause, the prior expectation quoted) in TEST-CONTRACT.md and `pipeline` voices `TEST_CHANGE_REQUESTED` (existing code; condition text names the Tester origin). On approval Tester amends **only the approved delta**, records the prior state, and the file joins Protected paths at the new anchor. Deletion, expectation weakening beyond the approved delta, and Developer ownership are never outcomes. The same route serves a conflict discovered at stage 6 (Developer origin).

**Execution.** Tester applies only the approved delta to only the named files (it is the sole writer of any amendment delta, on protected, adopted, or proof-relevant envelope paths alike); index-scope check; commit — the commit holding the amended content is the **candidate anchor**; contract command twice; per-test observed result per T11's package rule (`PASS — amended after implementation` is recorded as such, never RED). **Transition patch (C1):** with the candidate at HEAD and before it is recorded as the anchor, Tester runs `git -C <dir> diff <prior anchor>..HEAD -- <controlled paths>` — the full content patch over every controlled path (T9), including any path adopted into or removed from protection — and records it verbatim in the amendment entry, together with the staged-set record; `git -C <dir> diff --stat <prior anchor>..HEAD -- <controlled paths>` may accompany it as a summary but never replaces it. Both are read-only forms added to Tester's allowed commands (they prompt in Manual mode; `.vscode/settings.json` is unchanged). The entry names the approved delta, the transition patch, the dependency set, the prior and candidate anchors, and the controlled-path membership before and after. **The candidate becomes the active anchor only when the re-review (T11) finds that the transition patch equals the approved delta and nothing else** — every hunk accounted for, no extra change in an approved file, no change in an unapproved file, membership changes as approved; otherwise REVISE, and the candidate is superseded by a corrected commit with its own transition patch from the same prior anchor. A later diff from the new anchor never validates the transition into it. The same transition patch is recorded for every correction round (T11) from the superseded anchor. Every amendment, of every effect, is re-reviewed. RED-REPORT.md's Amendment re-proof carries the T6 shape for any test recorded RED.

**Proof-relevant envelope changes (M5, C3)** enter through this same path: Developer (agent) records the request; Tester assesses (`SUPPORT_ONLY` with the dependency set); the human decides; Tester applies the approved delta; transition patch; re-review. The Developer agent never edits a proof-relevant envelope file; without an approved amendment the Verifier fails the change (T9).

### T13 — Approved prerequisites (scaffold, library)

On `TEST_BOUNDARY_MISSING` resolved as "approve scaffolding as scope", the preparatory change (a test-scope dependency, a test facility) is made **by the developer as a human, on the feature branch, using the repository's existing facilities**, before Tester resumes; RUN.md records the decision and the resulting commit SHA `[TOOL]`; Tester resumes stage 5 with the anchor after that commit; Developer later lists the file under Envelope changes citing the RUN.md decision; the Verifier classifies it ENVELOPE with that justification. No agent performs production implementation before RED, and no new stage exists.

### T14 — Proportionality

Reuse the Phase 2 change class; no new classification. SMALL: one row per AC and `P` (or `COVERED BY`), usually one level, Doubles typically `none`, a four-line self-check, one compact review pass, no interface table, second run always. MEDIUM: adds boundary values where a criterion names a limit/count and every combined-outcome statement; bounded discretionary preservation; full self-check with `n/a — reason`. LARGE: adds T8's interface evidence per `I` row and levels by criterion. Every class: T3–T4, T6, T9, T12 lineage. Shared declarations replace repetition; empty sections collapse to one line.

### T15 — Policy skill and agent bodies

One skill, `.github/skills/test-contract/SKILL.md` (`user-invocable: false`, `disable-model-invocation: true`), is the single normative home for T2–T10, T12's effect enum and authority rule, T14, and the challenge catalogue with per-row evidence expectations. Read explicitly by `tester` (construction, assessment, amendment) and by `adversary` in test-review mode (challenge section, after the construction rules). Agent bodies own role, inputs, outputs, procedure, STOPs, forbidden actions, ownership; AGENT-CONTRACTS.md owns inputs/outputs/tools and short invariants plus artifact representation; FLOW.md owns routing and budgets. Policy tables are cross-referenced, not copied.

### T16 — Model roles (documentation only)

MODEL-ROLES.md records: Tester and the Adversary's test-review mode are benchmark candidates (scored dimensions: assertion-target correctness, boundary/doubles, RED validity, incorrect-behavior discrimination); the review's independence is procedural — outcome-first derivation from PLAN.md, source inspection, a distinct catalogue — with Sonnet-5 on both sides and no claim of independent model errors; "compact pass", "rare STOP", and marginal value are hypotheses to measure, not results. No model change.

## 4. Artifact templates (representation)

**TEST-CONTRACT.md** (owner: tester) fixed headings: Contract map · Proves · Protected paths per repository · Envelope per repository (with proof-relevant marks and the effect that makes an entry proof-relevant) · Relied-on existing tests (identity, covers, observed baseline `[TOOL]`) · Anchor per repository (active anchor SHA; staged-set record `[TOOL]`) · Contract command per repository (with the discovered-identity confirmation) · Interface evidence (LARGE with `I` rows; else one line) · Coverage gaps (or `none`) · Pre-existing test conflicts (or `none`) · Contract self-check · Correction rounds (superseded anchor, candidate anchor, transition patch `[TOOL]`, what changed; or `none`) · Amendments (per entry: request origin and content, assessment, decision, approved delta, prior and candidate anchors, transition patch `[TOOL]`, staged-set record, controlled-path membership before/after, dependency set, observed result per amended test, review outcome that activated the anchor; or `none`).

**RED-REPORT.md** (owner: tester): Command per repository (identities discovered/executed, both runs) · RED evidence (T6 six-line shape per WITNESS) · Preservation results (unchanged, plus relied-on baselines) · Evidence excerpt · Confirmation · Amendment re-proof.

**TEST-REVIEW.md** (owner: adversary, new): Verdict (`ACCEPT`/`REVISE`) · Basis · Required outcomes and incorrect behaviors derived (per clause, from PLAN.md) · Checks performed (catalogue rows with what was inspected and the result, recorded even when nothing is found) · Findings · Residual findings · Round (`1`, `2`, `H1` for a human-directed round; re-reviews append `-a<n>` per amendment).

**IMPLEMENTATION.md** (owner: developer): Test-change requests entries carry T12's request content; Envelope changes distinguish proof-relevant entries (which require an amendment id).

**VERIFICATION.md** (owner: verifier): Verdict `PASS`/`INCOMPLETE`/`FAIL`; Contract test run adds relied-on identity execution and the `NOT_VERIFIED` clause set derived from Coverage gaps plus RUN.md authorized exceptions, each with its source; Test immutability check anchored at the active anchor over Protected paths; Execution envelope check flags a proof-relevant change without an approved amendment as FAIL; Test review basis check (a current review per T11: `CURRENT ACCEPT` with matching basis, or `CURRENT REVISE` fully covered by authorized exceptions — the latter caps the verdict at `INCOMPLETE`).

**RUN.md** (owner: pipeline): stage 5b row in the Stage status table (round: `1`, `2`, `H1`, re-review ids); TEST-REVIEW.md in Artifact history; authorized exceptions (clauses, source, covered finding ids) and the `RUNNER_UNAVAILABLE` proceed-with-gap, `TEST_REVIEW_REVISE_LIMIT`, `VERIFICATION_INCOMPLETE`, `TEST_CHANGE_REQUESTED` (with origin and assessment effect) decisions and T13 prerequisite commits recorded under the existing decision-recording convention.

**FLOW.md** additions: stage 5b (Test review — adversary — reads skill, PLAN.md, TEST-CONTRACT.md, RED-REPORT.md, controlled paths — writes TEST-REVIEW.md — REVISE → tester once; 2nd REVISE → `TEST_REVIEW_REVISE_LIMIT`); STOP catalogue +2 (`TEST_REVIEW_REVISE_LIMIT`, `VERIFICATION_INCOMPLETE`); Bounded loops +1 ("Adversary/Tester (test review): one initial review plus at most one automatic correction round; `TEST_REVIEW_REVISE_LIMIT` on the second REVISE; a human-directed round is explicit and bounded, never an automatic reset"); Resume list: twelve artifacts; resume lands at 5b when TEST-REVIEW.md is missing or SUPERSEDED, at 5 when TEST-CONTRACT.md is superseded by a correction or by a recovery decision, and **never at 7 alone on `INCOMPLETE`** — a `VERIFICATION_INCOMPLETE` close-the-gap decision supersedes TEST-CONTRACT.md's affected evidence in the same RUN.md edit so the run returns through 5 → 5b → (6) → 7; stage 6 never starts without a current review (T11).

## 5. Files

**Modified:** `.github/agents/tester.agent.md`, `adversary.agent.md`, `developer.agent.md` (protected/proof-relevant wording; request content; authority rule), `verifier.agent.md` (Protected paths; relied-on execution; proof-relevant FAIL; `INCOMPLETE`; review-basis check), `pipeline.agent.md` (stage 5b; assessment step; Tester-origin `TEST_CHANGE_REQUESTED`; authorized-exception recording; `VERIFICATION_INCOMPLETE` with recovery through stage 5; no G4 on `INCOMPLETE`; T13 recording; twelve artifacts), `.github/pipeline/FLOW.md`, `AGENT-CONTRACTS.md` (tester/adversary/developer/verifier/pipeline sections; templates above; ownership matrix +1; Tester's added read-only forms `git -C <dir> diff <anchor>..HEAD -- <paths>` and `git -C <dir> diff --stat <anchor>..HEAD -- <paths>`), `GUARDRAILS.md` (Test immutability mechanism: protected/envelope/proof-relevant/relied-on; least-privilege tester row; Bounded loops), `MODEL-ROLES.md` (T16), `.github/skills/pipeline/SKILL.md` (twelve artifacts; authorized regenerations gain "tester correction round, test re-review"), `docs/COMPARISON-GUIDE.md`, `README.md`, `docs/examples/PAYMENTS-12410/` (extended in place through stage 5b, D5: TEST-CONTRACT.md, RED-REPORT.md, TEST-REVIEW.md, RUN.md rows, README boundary line linking the Phase 2 tagged version).

**Added:** `.github/skills/test-contract/SKILL.md`; `docs/examples/PAYMENTS-12345-testing/` (LARGE, on the Phase 2 plan `1.2`: README with the non-validation caveat toward both existing PAYMENTS directories, RUN.md through 5b, PLAN.md copied verbatim with a note, TEST-CONTRACT.md, RED-REPORT.md, TEST-REVIEW.md, and **focused fictional source excerpts** of the tests and helpers sufficient to assess the policy — the AC4 persistence path, the clock control, the `I1` adapter and decoding evidence).

**Not modified:** `docs/examples/PAYMENTS-12345/` (byte-identical), `docs/examples/PAYMENTS-12345-planning/`, `planner`, `intake`, `workspace`, `pr` agents, `plan-grounding`, `challenge-plan`, the Phase 1 skills `gather-jira-context` and `discover-affected-projects` (the third Phase 1 skill, `pipeline`, **is** modified as listed above — the only Phase 1 skill Phase 3 touches), `.github/copilot-instructions.md`, `.vscode/*` (the Tester's added diff forms are not auto-approved; they prompt), both historical contracts, the Phase 2 proposal, `docs/specs/archive/`, `docs/reviews/`, both reference tags.

## 6. Checkpoints and acceptance

**P3-C1 — Policy skill, contracts, agents.** Files: the skill; tester, adversary, developer, verifier, pipeline agents; FLOW.md, AGENT-CONTRACTS.md, GUARDRAILS.md, MODEL-ROLES.md, `skills/pipeline/SKILL.md`.
Acceptance: (a) every rule T1–T16 lives in exactly one normative place and the five agent bodies reference rather than restate it; no contradiction across the skill, the four pipeline documents, the pipeline skill, and the five agent bodies — in particular the protected/envelope/proof-relevant/relied-on definitions, the effect enum, the authority rule, the `NOT_VERIFIED`/`INCOMPLETE` semantics, the review basis rule, the STOP wording, and the bounded-loop sentence; (b) every `tools:` list byte-identical to `5a46eb7`; no new agent, gate, tool, MCP capability, or setting; exactly one new artifact and two new STOP codes; existing STOP messages unchanged; (c) T3's outcome table, T4's principle and five prohibitions (with the input-stub allowance), T5's four bases and relied-on obligations, T6's six-line shape and six validity rules, T7 with the bounded-time distinction, T8's three evidence kinds and conformance values, T9's effect-based proof-relevance rule and controlled-paths definition, T10's equivalent/partial distinction, authorized-exception definition, `INCOMPLETE` consequences and recovery-through-stage-5 rule, T11's outcome-first method, one package rule (pre- and post-implementation), basis, "current review" definition, dependency-set re-review, T12's enum with `REQUIREMENT_CHANGE` never amendable, candidate-anchor and content transition-patch rule, Tester as sole amendment writer, Tester-origin route, T13's prerequisite rule, and the challenge catalogue with evidence expectations are each present and quotable; (d) stage 6 cannot start, and G4 cannot be asked, without a current review or on `INCOMPLETE`; recovery from `INCOMPLETE` returns to stage 5 and never to stage 7 alone — stated identically in `pipeline.agent.md`, FLOW.md, and the pipeline skill; the Verifier's basis check and verdict rule agree with T10/T11 word for word on the two current-review forms; (e) the Verifier's immutability command form unchanged; only its path input and the envelope/relied-on inputs change; the Tester's two added read-only diff forms appear in AGENT-CONTRACTS.md and `tester.agent.md` only; (f) `planner`, `intake`, `workspace`, `pr`, the two Phase 2 skills, `gather-jira-context`, `discover-affected-projects`, `copilot-instructions.md`, `.vscode/*`, both historical contracts, `docs/examples/PAYMENTS-12345/` and `PAYMENTS-12345-planning/` unchanged (`git diff` empty); the `pipeline` skill changed only in the artifact list and the authorized-regeneration sentence.

**P3-C2 — Test-stage examples.** Files: `docs/examples/PAYMENTS-12345-testing/`; `docs/examples/PAYMENTS-12410/` extended.
Acceptance: (a) LARGE: every AC1–AC5 clause and P1–P3 in the map (AC5's three combined cases; AC2 at the boundary attempt `maxAttempts`); AC1–AC3/AC5 at `SERVICE` with the real retry path and the real repository mapping against `EMBEDDED` storage (new field and new enum value persisted — T3), AC4 at `API` with real decoding and persistence asserted; Doubles limited to the ledger transport edge and the clock; T6 evidence with two runs; `I1` with consumer adapter evidence at the transport edge, provider decoding evidence, `Conformance: ESTABLISHED` (or an honest lesser value with the reason); `FakeClock` and fixtures protected; `pom.xml` and `application-test.yml` classified proof-relevant by inspection of their fictional content with the effect (or its absence) stated — no classification is prescribed by file type; P2 `COVERED BY` a relied-on test with an observed baseline if E10 supports it, else a bounded test with basis; staged-set record; self-check; TEST-REVIEW.md with basis, derived incorrect behaviors, checks, and findings only where honestly present; focused source excerpts; RUN.md through 5b; README caveats. (b) SMALL: two map rows plus one `P` (or `COVERED BY` with baseline); AC1 at `API` asserting the error body names the field; Doubles `none`; four-line self-check; TEST-REVIEW.md ACCEPT with basis and checks; a source excerpt of the AC1 assertion; TEST-CONTRACT.md under ~50 lines with normal spacing (soft). (c) Every identifier and log fictional and stated so; every RED excerpt an assertion failure inside a test method; no example claims execution that did not occur.

**P3-C3 — Guide, README, closure.** Files: `docs/COMPARISON-GUIDE.md`, `README.md`, §7 below.
Acceptance: (a) guide entries for the skill, the tester/adversary/developer/verifier/pipeline changes, the two examples, the Phase 1 example's differing helper policy, and a cross-cutting theme "Executable definition of done"; README asset tree and status current; (b) §7 completed by Fable with the challenge cases traced as document scenarios against the delivered files.

## 7. Closure evidence — challenge cases (completed by Fable at P3-C3)

| Case | Expected outcome | Where the rule lives | Traced |
|---|---|---|---|
| A WITNESS doubles the responsible component or checks only invocation | HIGH at 5b; REVISE; corrected | T4, T11 | **Traced (P3-C2 at `4750ddf`, `docs/examples/PAYMENTS-12345-testing/`).** The skill's T4 prohibitions (1), (3), and (4) name the defect; the Challenge catalogue row "Boundary and doubles (T3/T4)" requires the Doubles column and the code to agree with the responsible component real; severity HIGH ("the responsible path doubled") forces REVISE. The example exercises it: round-1 `recordsRetryAttemptInAuditLog` doubled `AuditLogRepository` with a `@MockBean` recorder and verified only the shape of the `.save(...)` invocation — TEST-REVIEW.md F1 (HIGH, T4 prohibition 4), Verdict REVISE at Round 1; `pipeline` re-invoked `tester` for the one automatic correction round (`pipeline.agent.md` step 5; FLOW.md stage 5b); round 2 asserts through the real repository against the embedded PostgreSQL with the persisted row checked first; TEST-CONTRACT.md's Correction rounds records the superseded and active anchors with the verbatim transition patch; TEST-REVIEW.md Round 2 is ACCEPT with F1 resolved; RUN.md's Artifact history shows round-1 TEST-REVIEW.md `SUPERSEDED (REVISE)` and round 2 `ACTIVE (ACCEPT)`. The same example's AC5(i) row shows the invocation-only half of the case avoided by rule: the ledger `MockWebServer` is the recorder and the decoded request bodies (`deliveryId`, `attemptNumber`, `outcome`) are asserted, never a bare request count (prohibition 3). |
| API returns 200/persists via a fake while the real mapping is wrong | Level insufficient for a persisted outcome; HIGH | T3, T11 | **Traced at `9b78efb` and in the LARGE example.** The skill's T3 outcome table, row "Persisted outcome": sufficient evidence is the real repository/mapping code against the storage the repository's tests already use (`EMBEDDED`); a fake repository is listed as not sufficient because "it proves the intent to save, not the mapping, transaction, or stored column". The catalogue rows "Level sufficiency (T3)" and "Assertion target vs Observed at" check the recorded level and the assertion against Observed at; HIGH ("a WITNESS whose assertion does not target the clause's outcome") forces REVISE. The example's F1 is exactly this scenario: round 1 asserted the HTTP 200 first and the recorder's `save` last, so an implementation calling `.save(row)` on the mocked bean and never touching PostgreSQL would have passed (SOURCE-EXCERPTS.md (h) states the counterexample); round 2 runs at `API` through the real route, the real `AuditLogRepository`, and zonky's embedded PostgreSQL, and TEST-CONTRACT.md classifies `payments-ledger/pom.xml` and `spring.test.database.replace: none` proof-relevant so that swapping the storage engine would itself be a controlled-path change (T9). |
| RED produced by a fixture error or an unarranged Given | Fails validity (2)/(3); corrected as a test defect; never RED | T6 | **Traced at `9b78efb`.** The skill's T6 validity rules (2) (failure at the test's own assertion on the clause's outcome) and (3) (the Given arranged as the Setup line states) exclude it, and T6 states outright that RED "caused primarily by … a broken fixture … or a runner failure is not RED". `tester.agent.md` step 7 investigates a WITNESS that does not fail for the acceptance behavior (at most two correction attempts, RED re-proven each time, never by weakening the criterion) and step 9 diagnoses a second-run discrepancy with the evidence preserved; the catalogue row "RED validity and setup (T6)" checks the six-line shape, the six rules, and the Setup line's arrangement against the quoted `[TOOL]` output, and HIGH names "RED at a non-assertion locus or with an unarranged Given". Both examples carry the six-line shape per WITNESS with the Setup and Validity lines and two consecutive runs per repository; the LARGE example orders each ledger request-count assertion first so its RED is a plain `AssertionFailedError`, never a blocked read. No fixture-error RED occurred in either example, so the case is traced as a rule, not as an example event. |
| Consumer recorder and provider hand-shaped JSON agree on the plan's field list while the real client serializes differently | `DESIGN_CHECK_ONLY`, never `ESTABLISHED`; interface clause `NOT_VERIFIED` unless adapter/decoding evidence exists | T8, T10 | **Traced at `9b78efb` and in the LARGE example.** The skill's T8 grants `ESTABLISHED` "only when both adapter and decoding evidence exist and agree", `DESIGN_CHECK_ONLY` "when only field-list agreement exists", and `NOT_VERIFIED` when a side has no executable evidence (T10 then applies to the interface clause); it states that "a domain-object recorder at the client interface is local business evidence for the consumer's decision, not interface evidence". The catalogue row "Interface evidence kind and conformance value (T8)" makes a field-list-only agreement recorded as `ESTABLISHED` a finding; HIGH names "interface conformance claimed beyond its evidence". The example's Interface evidence table earns `ESTABLISHED` the required way: consumer adapter evidence from `encodesRetryAttemptReportAtTheTransportEdge`, driving the real `RestLedgerClient` against a `MockWebServer` and asserting the encoded path and JSON; provider decoding evidence from `recordsRetryAttemptInAuditLog` through the real route and repository; the negative `retryReportTransportFailureLeavesAttemptUnreported` as deployment-compatibility evidence; TEST-REVIEW.md's T8 check names field-list-only agreement as what would have capped the value at `DESIGN_CHECK_ONLY`. The P3-C2 correction rounds replaced an object-level `LedgerClient` recorder — local evidence at best, and uncompilable at the anchor — with this transport-edge recorder. |
| Only a DB/broker level proves a clause and none is available | Equivalent evidence sought; else `NOT_VERIFIED`, `RUNNER_UNAVAILABLE`, proceed-with-gap; Verifier `INCOMPLETE`; no G4 | T10 | **Traced at `9b78efb`.** The skill's T10: equivalent evidence is explained in the Contract map row, challenged by the review, and never decided by the developer; otherwise the runnable test is partial evidence, the clause is recorded `NOT_VERIFIED (environment: …; partial evidence: …)` in the map and under Coverage gaps, and `RUNNER_UNAVAILABLE` is raised per clause; proceeding "is authorization of progress, never a finding of equivalence". `tester.agent.md` step 8 and its `RUNNER_UNAVAILABLE` entry carry the four developer options; `pipeline.agent.md` step 5 records proceed-with-gap as an authorized exception in RUN.md's Decision log naming the clause; `verifier.agent.md` derives the `NOT_VERIFIED` set from Coverage gaps plus still-current exceptions, renders `PASS` only when that set is empty, `INCOMPLETE` otherwise, and is forbidden to render `PASS` while the set is non-empty; `pipeline.agent.md` (Gates log, step 7, Forbidden actions), FLOW.md's stage-7 row and STOP table, and the pipeline skill state that G4 is never asked on `INCOMPLETE`; `developer.agent.md` step 3 implements a `NOT_VERIFIED` clause against the whole plan. The catalogue row "Coverage-gap and equivalence honesty (T10)" makes a non-equivalent substitute presented as equivalent HIGH. Neither example exercises this path (both record Coverage gaps `none` and say so in their READMEs). |
| A request replaces a persisted-state assertion with "client called" | `REQUIREMENT_CHANGE`; not amendable; planning cycle; contract superseded | T12 | **Traced at `9b78efb`.** The skill's T12 authority rule — an amendment "may change how a test observes; it may never change what the approved clause requires", effect measured against the PLAN.md clause — and its enum entry `REQUIREMENT_CHANGE` ("removes part of the approved outcome, changes success semantics, or accepts different behavior — not amendable: routed as a `SEND_BACK`-class decision to a new planning cycle, which supersedes the contract"). Replacing a persisted-state assertion (T3, "Persisted outcome") with an invocation check removes the required outcome, so `tester`'s assessment (Assessment mode) classifies it `REQUIREMENT_CHANGE`; `pipeline.agent.md` step 6 then voices the existing send-back decision instead of `TEST_CHANGE_REQUESTED`, and `tester`'s Amendment execution is "never for a `REQUIREMENT_CHANGE` effect"; `developer.agent.md` step 5 requires the request to say whether the required outcome is unchanged and routes a changed outcome to planning; GUARDRAILS.md's Test immutability mechanism and AGENT-CONTRACTS.md's pipeline, tester, and developer sections say the same. The new planning cycle supersedes PLAN.md, which marks TEST-REVIEW.md `SUPERSEDED` (RUN.md's Artifact history rule), and a superseded TEST-CONTRACT.md resumes at stage 5. Not exercised in an example. |
| A request removes an accidental overconstraint | `OVERCONSTRAINT_REMOVED`; applied; re-reviewed for discrimination | T12, T11 | **Traced at `9b78efb`.** The skill's T12 enum entry `OVERCONSTRAINT_REMOVED` ("the defective test demanded more than the clause; the amended test still proves the **entire** approved outcome — legitimate"); `tester.agent.md` Assessment mode records the effect, affected clauses, stale evidence, and a recommendation, `pipeline.agent.md` step 6 voices `TEST_CHANGE_REQUESTED` with that assessment attached (and is forbidden to voice it without), and on approval `tester`'s Amendment execution applies only the approved delta, commits the candidate anchor, and records the content transition patch and dependency set. `adversary.agent.md` test-review step 4 then re-reviews the affected dependency set (Round `-a<n>`) and, per the catalogue's re-review row, checks "the discrimination of amended or later-authored tests against plausible incorrect behaviors (step 1–2)"; on ACCEPT `tester` is invoked in activation mode, and `developer` resumes only then (`pipeline.agent.md` step 6; `developer.agent.md` Forbidden actions). Not exercised in an example — both READMEs state that no amendment occurred. |
| A `SUPPORT_ONLY` change to a shared fake | Dependency set named; review SUPERSEDED; re-review of the set; stage 6 waits for a current review | T11, T12 | **Traced at `9b78efb`.** A shared fake is a controlled path either way — a tester-created support file is a Protected path, a pre-existing one is envelope marked proof-relevant by effect ("a double's semantics", T9) — so the change is an amendment, never a developer envelope change. The skill's T12 enum entry `SUPPORT_ONLY` ("support/configuration change with the dependency set named"); `tester.agent.md` Amendment execution names in the entry "the dependency set (every test depending on the changed support or configuration, and any coverage or gap changed)"; `pipeline.agent.md`'s Artifact history rule marks TEST-REVIEW.md `SUPERSEDED` "in the same edit as any Tester correction, amendment, proof-relevant change …"; `adversary.agent.md` is re-invoked "over the **affected dependency set** named in the amendment entry, not necessarily every test", and the catalogue's re-review row checks "the recorded dependency set for completeness"; the pre-stage rule in `pipeline.agent.md` (identical in FLOW.md and the pipeline skill) states that stage 6 cannot start without a current test review, resume lands at stage 5b while TEST-REVIEW.md is SUPERSEDED, and `developer.agent.md` is forbidden to resume against an amended contract before `pipeline` confirms a current review. Not exercised in an example. |
| An approved one-literal fixture correction also weakens another assertion in the same file | Transition patch shows the extra hunk; re-review REVISE; candidate never becomes the anchor | T12, T11 | **Traced at `9b78efb`, with the hunk-accounting method shown in the LARGE example.** The skill's T12 (C1): the full content patch over every controlled path is recorded verbatim, a `--stat` summary "never replaces it", and "the candidate becomes the active anchor only when the re-review finds that the transition patch equals the approved delta and nothing else — every hunk accounted for, no extra change in an approved file …; otherwise REVISE, and the candidate is superseded by a corrected commit with its own transition patch from the same prior anchor". The catalogue's re-review row compares the patch hunk by hunk; HIGH names "a discriminating check failing on re-review"; `tester.agent.md` is forbidden to record a candidate as active before acceptance and activation, or to replace a transition patch with a `--stat` summary; `verifier.agent.md` step 7 fails an active anchor that differs from the review Basis's candidate, and `pipeline.agent.md`'s Resume by artifact treats an accepted-but-unactivated candidate as an incomplete TEST-CONTRACT.md that resumes at the activation step, never at stage 6. The example applies the same method to its correction round: TEST-CONTRACT.md's Correction rounds records the verbatim three-hunk transition patch with its `--stat` companion, and TEST-REVIEW.md Round 2's "On re-review" check accounts for each hunk and confirms the hunk headers and stat totals match the bodies — the check that would expose an extra hunk. The stage-5 adoption analogue is stricter still: the bare `git diff` must contain only the approved delta, or `tester` does not commit (`tester.agent.md` step 4). |
| Developer edits a pre-existing proof-relevant fixture, or a build property selecting a fake storage adapter, with an Envelope justification | Proof-relevant by effect; Verifier FAIL absent an approved amendment | T9 | **Traced at `9b78efb` and in both examples.** The skill's T9 marks each envelope entry "proof-relevant: yes/no by effect, never by file type" — naming "provider or adapter selection, storage or profile selection, fixture business state, a double's semantics" — and states that a change to a proof-relevant file "is not an ordinary envelope change: it goes through the amendment path (T12) …; the Verifier treats a proof-relevant change with no approved amendment as FAIL regardless of the Envelope changes justification". `developer.agent.md` (Role and purpose, steps 2 and 5) treats a proof-relevant file as a controlled path and raises it as a test change; `verifier.agent.md` step 6 and its Execution envelope check heading render FAIL regardless of justification; AGENT-CONTRACTS.md's IMPLEMENTATION.md template says a proof-relevant file "is never listed" under Envelope changes; GUARDRAILS.md control 2 restates it. The examples classify by inspection: the LARGE contract marks `payments-api/pom.xml` **no** (versions only) but `payments-ledger/pom.xml` **yes** (the `embedded-database-spring-test` dependency selects the storage engine) and both `application-test.yml` files **yes** (the H2 datasource URL and retry values; `spring.test.database.replace: none`) — the build-property-selecting-a-storage-adapter case in the row — and the SMALL contract marks its `application-test.yml` **yes** for the datasource property and `pom.xml` **no**; each TEST-REVIEW.md checks the classification by effect. |
| Developer proceeds after `TEST_REVIEW_REVISE_LIMIT` | Authorized exception in RUN.md; clauses `NOT_VERIFIED`; Verifier recognises the covered REVISE as current and renders at most `INCOMPLETE`; no G4 | T10, T11 | **Traced at `9b78efb`.** `adversary.agent.md`'s `TEST_REVIEW_REVISE_LIMIT` text offers three options, and `pipeline.agent.md` step 5 records "proceed with the affected clause(s) recorded `NOT_VERIFIED`" as "an authorized exception recorded in RUN.md's Decision log naming the clauses and the covered finding ids" (the Decision log bullet under Owned artifacts lists the same fields). The skill's T10 defines the exception ("proceed at `TEST_REVIEW_REVISE_LIMIT` (proof defect)"), which "never marks evidence equivalent, never reclassifies a test". `verifier.agent.md` (Role and purpose, step 7) accepts a `CURRENT REVISE` "whose every open HIGH finding is covered by an authorized exception" as a current review that "never allows PASS, only caps the verdict at `INCOMPLETE`" and is forbidden to render `PASS` on it "however well covered"; `pipeline.agent.md`'s pre-stage rule counts the covered REVISE as a current review for stage 6 while G4 is never asked on `INCOMPLETE`; FLOW.md's STOP table carries both rows. Not exercised in an example — the LARGE RUN.md's Decision log records that no `TEST_REVIEW_REVISE_LIMIT` occurred. |
| Environment becomes available after `INCOMPLETE` | Recovery at stage 5: deferred test authored, result recorded honestly (`PASS — authored after implementation` or RED → stage 6), re-review, then stage 7 | T10, T11 | **Traced at `9b78efb`.** `verifier.agent.md`'s `VERIFICATION_INCOMPLETE` text; `pipeline.agent.md` step 7: "recovery always returns to the earliest missing evidence — stage 5 …, then 5b …, then, on `ACCEPT`, invoke `tester` in activation mode …, then 6 only if a recorded RED requires implementation, then 7 again — resuming at stage 7 alone can never close a gap"; `tester.agent.md`'s "Recovery from `INCOMPLETE`" paragraph authors the deferred test and records the result honestly — `RED` (back through stage 6) or `PASS — authored after implementation` per the one package rule, classification unchanged unless a recorded developer decision changed it — as a new entry with its own transition patch, re-reviewed at 5b and activated on ACCEPT. Closing the gap retires the exception from the current `NOT_VERIFIED` set as a new Decision log entry with the original kept as history (`pipeline.agent.md` Owned artifacts and step 7), and `verifier.agent.md` derives the set only from still-current exceptions. The stage-6/G4 sentence and the recovery-through-stage-5 rule are stated identically in `pipeline.agent.md`, FLOW.md's Resume by artifact, and the pipeline skill (P3-C1 acceptance (d)). Not exercised in an example. |
| A relied-on existing test is disabled | Cannot supply coverage at stage 5; missing at verification → FAIL | T5 | **Traced at `9b78efb` and in the LARGE example.** The skill's T5 requires a relied-on test to be run at stage 5 through the runner scoped to its class with the observed baseline and scoped command recorded, states that one "that is disabled, undiscovered, or failing at baseline cannot supply coverage" (the clause then gets its own test or a `PRESERVATION_BASELINE_FAILED` diagnosis), and requires observed execution of every relied-on identity at stage 7 with missing, skipped, or failing as FAIL; T9 diffs relied-on tests since the anchor like envelope files. `tester.agent.md` step 4 runs and records the scoped baseline; `verifier.agent.md` step 3 requires each identity observed in (A) or (B) or in a scoped fallback run ("a passing aggregate never implies its execution") and step 6 includes relied-on tests in the envelope diff; FLOW.md's stage-7 row and AGENT-CONTRACTS.md's verifier section say the same. The LARGE example records `AuditLogRepositoryTest#persistsPaymentCapturedEventPayload` covering P2 with its scoped command and observed PASS, checked by TEST-REVIEW.md's T5 row; the SMALL example records `none` with the reason. A disabled relied-on test is not shown as an event. |
| A pre-existing test encodes obsolete behavior | Tester-origin `TEST_CHANGE_REQUESTED`; approved delta only; prior state recorded; file adopted into protection | T12 | **Traced (P3-C2 at `4750ddf`, `docs/examples/PAYMENTS-12345-testing/`).** `tester.agent.md` step 4 defines the route: a contradicting `ADJACENT_TEST` is recorded under Pre-existing test conflicts, every other clause is constructed first, no RED run or commit happens while the conflict is outstanding, control returns to `pipeline`, which follows the assessment step and voices the Tester-origin `TEST_CHANGE_REQUESTED` wording (`tester.agent.md` STOP conditions; `pipeline.agent.md` step 5); on approval `tester` applies the delta and finishes stage 5 in the same invocation, the transition evidence being the bare `git -C <dir> diff` taken before any `git add`, and the adopted file joins Protected paths at the RED commit, which is itself the anchor (skill T12, adoption paragraph; AGENT-CONTRACTS.md tester section; GUARDRAILS.md "or, for a pre-existing test conflict, the tester"). The example exercises every step: `WebhookDeliveryServiceTest#failedDeliveryIsMarkedFailedAndNotRetried` is recorded under Pre-existing test conflicts with the prior expectation quoted, the approved `ADOPTION` delta (rename; delete the obsolete `FAILED`-terminal assertion and, in retargeting the test to first-attempt recording, the immediate request-count assertion; keep the outcome-`FAILURE` assertion), the verbatim bare `git diff` (one hunk, only the approved path), and a staged-set record showing the adopted path as `1 M.` beside the created files' `1 A.`; Protected paths lists it as immutable from the round-1 anchor; RUN.md's Decision log records the Tester-origin `TEST_CHANGE_REQUESTED` with the assessment, the `[DEV]` decision, its basis, and `CURRENT`; the adopted test is PRESERVATION (basis 3) with PASS on both runs; TEST-REVIEW.md checks it under the protected/envelope split. |
| Review correction or interruption | Old RED/review SUPERSEDED; resume at 5 or 5b, never 6 | T11 | **Traced at `9b78efb` and in the LARGE example.** `pipeline.agent.md`'s Resume by artifact: "TEST-REVIEW.md missing or SUPERSEDED resumes at stage 5b; TEST-CONTRACT.md superseded by a correction or by a recovery decision resumes at stage 5; stage 6 never resumes without a current test review" — identical in FLOW.md's Resume by artifact and the pipeline skill; `tester.agent.md`'s Correction round: "Prior RED/review evidence for the corrected tests is superseded in the same TEST-CONTRACT.md edit"; `adversary.agent.md`: any correction, amendment, proof-relevant change, coverage/limitation change, or PLAN.md supersession marks the prior TEST-REVIEW.md SUPERSEDED and the adversary is re-invoked; RUN.md's Artifact history rule records the supersession in the same edit. The example's RUN.md shows the bookkeeping for a correction: round-1 TEST-CONTRACT.md and RED-REPORT.md `SUPERSEDED`, TEST-REVIEW.md round 1 `SUPERSEDED (REVISE)`, round 2 `ACTIVE (ACCEPT)`, Resume notes "not interrupted"; an interruption between the correction commit and its re-review would land at 5b by the rule above. Not shown as an interruption event. |
| A genuinely small change | Short contract, four-line self-check, one compact pass, silent ACCEPT | T14 | **Traced (P3-C2 at `4750ddf`, `docs/examples/PAYMENTS-12410/`).** The skill's T14 SMALL column (one row per AC and `P`, usually one level, doubles typically `none`, `P` rows only, no interface evidence, four-line self-check, second run always, one compact stage-5b pass) and its Challenge section ("a SMALL contract gets one compact pass"; a zero-finding ACCEPT with checks recorded is legitimate); `adversary.agent.md` and `pipeline.agent.md` step 5: "ACCEPT is silent — Developer proceeds without a prompt". The example: a three-row Contract map (AC1 WITNESS parameterized over CREATE and UPDATE, AC2 PRESERVATION basis 2, P1 PRESERVATION basis 1), every row `API`, Doubles `none`, Determinism `none`; Interface evidence one line; Relied-on existing tests `none` with the reason; the four-line self-check; Correction rounds and Amendments `none`; TEST-REVIEW.md Round 1 ACCEPT with zero findings, Checks performed recorded, Residual findings `none`; RUN.md stage 5b `COMPLETE — ACCEPT`, Decision log `none`. One acceptance figure is reported as measured rather than met: TEST-CONTRACT.md is 69 lines with normal spacing against the soft ~50-line target — thirteen fixed headings make the target unattainable without collapsing the shape; content, not length, is what T14 keeps small (as with the Phase 2 SMALL plan). |

## 8. Owner decisions

**Applied as qualified.** D1: bounded Adversary review for every class with source inspection, its own artifact, a basis, freshness and dependency-set re-review, no routine gate; proof defects never represented as verification. D2: reuse unchanged tests directly; approved-delta adoption of an obsolete pre-existing test with a Tester-origin route at stage 5; Developer never edits tests. D3: equivalent evidence accepted after review; partial evidence never promoted; `NOT_VERIFIED` clause with `INCOMPLETE` downstream. D4: concise assessment against PLAN.md, not a form; the re-review supplies independence. D5: PAYMENTS-12410 extended in place, stopping at test review, linking the Phase 2 tagged version.

**D6 — publication with an accepted coverage gap: decided (a) by the owner, 2026-09-10.** `INCOMPLETE` never reaches G4; the gap is closed by recovery through stage 5, changed through planning, or the run is aborted; the deferred test's design is recorded for an environment that can run it. A qualified publication path (a distinct G4 answer and PR qualification) is deferred, not designed; corporate policy and facilities can be compared separately.

**No owner decision remains open in this contract.**

## 9. Deferred

Exact Jira/Bitbucket MCP names, the Sonnet-5 picker string, the agent-picker smoke check, corporate approval-engine behavior, model benchmarks, measurement of stage 5b's actual marginal value, and housekeeping (normative repetition across skill/contract/agents). Phase 4 owns implementation strategy, GREEN iteration, and the Verifier's broader semantic review.

## 10. Phase 3 closure (Fable, 2026-09-11)

**Delivered.** P3-C1 at `92540f7` (the `test-contract` policy skill; tester, adversary in test-review mode, developer, verifier, pipeline; FLOW.md, AGENT-CONTRACTS.md, GUARDRAILS.md, MODEL-ROLES.md, the pipeline skill), with its targeted correction at `9b78efb` (stage-5 adoption staged into the RED anchor, C1-I1; Tester activation mode for accepted candidate anchors, C1-I2; stage-5b invocation scoped to test-review inputs, C1-I3). P3-C2 at `4750ddf` (the LARGE test-stage example `docs/examples/PAYMENTS-12345-testing/` and `docs/examples/PAYMENTS-12410/` extended in place through stage 5b). P3-C3 in the closure commit (comparison guide, README, the §7 traces, this section). Each checkpoint was implemented by Sonnet 5 (dispatched with `model: "sonnet"`; the P3-C3 implementer self-reported `claude-sonnet-5`) and reviewed independently in the architect role against §6 before commit; Astra reviewed P3-C1 and P3-C2 independently after commit, and P3-C3 and the assembled phase await Astra's final review. P3-C1 needed one Sonnet correction round before commit, and one post-review targeted correction (`9b78efb`) implemented by Sonnet 5 with five one-sentence consistency edits by Fable. P3-C2 needed one post-review correction round implemented by Sonnet 5 under Fable supervision and a second, four-file correction that Fable applied directly — a departure from the Sonnet-implements/Fable-reviews division recorded here as a process deviation, never described as Sonnet-authored; Astra's final closure review and checkpoint confirmation found the committed content identical to the reviewed tree and P3-C2 closed. P3-C3 needed two Sonnet correction rounds before commit, the README none: five guide findings in the first — a claim that the Tester's two transition-patch diff forms prompt (carried into the implementation brief from §3/§5's wording and contradicted by the delivered files, see clarification (3) below), a misquoted pipeline-skill phrase, the Amendments and Amendment re-proof sections described as added rather than reshaped, adoption wording the P3-C2 correction had already removed, and a misquoted least-privilege row — and, in the second, one omission the first round's rewording introduced (the reshaped Amendments list dropped the observed result per amended test). The architect role changed model during P3-C3: Fable 5.1 recovered the scope, briefed Sonnet 5, reviewed its first delivery, and authored §7 and this section; the owner then switched the session to Opus 5, which continued the same role — verifying both correction rounds, completing this section, making the closure commit, and performing the assembled conformance review.

**Review findings that shaped the delivered files.** P3-C1 (Astra, `docs/reviews/2026-09-10-phase-3-c1-implementation-review.md`): the approved stage-5 adoption delta was not staged into the RED anchor (fixed by naming the adopted path in the index-scope check's intended set and recording the bare `git diff` before any `git add`); no handoff activated an accepted candidate anchor (fixed by Tester activation mode, an artifact-only edit that never supersedes the review); the orchestrator's INTAKE/RUN convention leaked into the stage-5b prompt (fixed by scoping the test-review invocation to that mode's inputs only). P3-C2 (Astra, `docs/reviews/2026-09-10-phase-3-c2-implementation-review.md` and the two closure reviews of 2026-09-10 and 2026-09-11): AC5(i)'s re-report evidence was a request count with an impossible baseline, then a body multiset that omitted the delivery id — fixed by decoding `(deliveryId, attemptNumber, outcome)` with the first report's content asserted after step 1 and the two later reports as an unordered multiset; the terminal tests relied on the driver's iteration bound — fixed by a controlled subsequent check (FakeClock advanced past the largest delay, the entry point called again, counts and `next_attempt_at` asserted); the `I1` negative evidence sat on the wrong operation and the positive case never configured a ledger success — fixed by a real-adapter transport-failure test and an explicitly enqueued ledger 200; the SMALL example omitted the update path — fixed by parameterizing AC1/AC2 over CREATE and UPDATE; and the real-transport substitution introduced a finite ledger response queue that legal re-reports would exhaust — fixed by an always-rejecting `Dispatcher`, a reserved merchant response for the post-terminal probe, and stated per-test mock isolation. The adoption rationale was simplified so the immediate request count is neither presented as contradicting backoff nor as proof that no further attempt occurs.

**Acceptance.** §6 P3-C1 (a)–(f), P3-C2 (a)–(c), and P3-C3 (a)–(b) verified from the files: every `tools:` list byte-identical to `5a46eb7`; exactly one new artifact (TEST-REVIEW.md) and two new STOP codes with existing messages unchanged; the stage-6/G4 sentence and the recovery-through-stage-5 rule identical in `pipeline.agent.md`, FLOW.md, and the pipeline skill; the Verifier's immutability command form unchanged; `planner`, `intake`, `workspace`, `pr`, the two Phase 2 skills, the two Phase 1 intake skills, `copilot-instructions.md`, `.vscode/*`, both historical contracts, and `docs/examples/PAYMENTS-12345/` and `PAYMENTS-12345-planning/` unchanged. The sixteen §7 challenge cases are traced above: five are exercised by the examples (the doubled responsible component and the fake-persistence API case, both by round 1's AC4 test; the adoption of an obsolete pre-existing test; the correction-round supersession bookkeeping; and the SMALL contract), the rest are traced as rules against the delivered agent bodies, skill, and pipeline documents, with the example cited wherever it shows the method. One acceptance figure is reported as measured rather than met: the SMALL example's TEST-CONTRACT.md is 69 lines with normal Markdown spacing against the soft ~50-line target — thirteen fixed headings make the target unattainable without collapsing the artifact shape; content, not length, is what T14 keeps small.

**Owner decisions applied as qualified.** D1–D6 as recorded in §8; no owner decision remains open.

**Accepted contract clarifications (recorded here; the contract text above is left as approved).** (1) RUN.md's **Decision log** heading, owned by `pipeline`, is the "existing decision-recording convention" §4 refers to for authorized exceptions, T13 prerequisite commits, `TEST_CHANGE_REQUESTED` decisions, and `VERIFICATION_INCOMPLETE` choices — the same statement/answer/basis/status fields as the gates log; recorded in `docs/questions.md` at P3-C1. (2) The pipeline skill's Resume by artifact paragraph carries the Phase 3 resume landings (TEST-REVIEW.md → 5b, superseded TEST-CONTRACT.md → 5, unactivated candidate → activation step) and the stage-6/G4 sentence — a deviation from §6(f)'s "changed only in the artifact list and the authorized-regeneration sentence", required by §6(d)'s "stated identically in `pipeline.agent.md`, FLOW.md, and the pipeline skill". (3) §3 T12 and §5 say the Tester's two added read-only diff forms "prompt in Manual mode"; they do not: `.vscode/settings.json` (unchanged, consistent with §6(f)) already auto-approves `git -C <dir> diff <sha>..HEAD -- <paths>` and its `--stat` form for a hex-SHA anchor (rules at lines 29 and 33), and `tester.agent.md` states this correctly — "The four diff forms match existing allow rules in `.vscode/settings.json`; only the runner prompts in Manual mode". The delivered agent body, not the contract sentence, is the accurate description. (4) The Phase 2 planning example's approved PLAN.md `1.2`, consumed unchanged by the LARGE test-stage example, omits `LedgerClient`/`RestLedgerClient` from its operative Affected files although its Approach and `I1` route the report through them; Astra recorded this as a pre-existing planning-inventory gap, not a Phase 3 regression. The locked plan is left untouched; the Tester noticing such a gap surfaces it for planning through the existing STOP routes rather than treating later Verifier discovery as a substitute. (5) Optional response-fixture housekeeping the reviews left non-blocking — fail-fast or dispatcher defaults for the other post-terminal probes (AC2, AC3, AC5(iii), and the merchant side), where an incorrect extra request would fail by client timeout rather than at the clean count assertion — was not done.

**Not validated.** No Copilot session ran these agents; the examples are fictional document scenarios and say so. Corporate validation items in §9 remain deferred. The Phase 1 reference tag `phase-1-reference` still resolves to `03d4230` and the Phase 2 reference tag `phase-2-reference` to `5a46eb7`; no Phase 3 tag exists. This closure records checkpoint delivery only; the assembled Phase 3 conformance review follows the closure commit and is reported to the owner, not recorded here. Astra's final independent review, push, lock, and any Phase 4 work are each separately authorized.
