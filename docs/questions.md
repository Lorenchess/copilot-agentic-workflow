# Implementation Questions

Implementers record open questions raised while working a checkpoint here instead of editing the approved contract (`docs/specs/2026-09-09-phase-1-architecture-contract.md`).

| Checkpoint | Question | Raised by | Status | Answer |
|---|---|---|---|---|
| C1 | Exact Sonnet-5 model picker string in the work environment? | Sonnet | Deferred (needed only when running at work) | |
| C1 | Jira MCP server name as shown in the tools picker, and the exact read tool names for: get issue with field names, list comments, list links, JQL search? | Sonnet | Deferred (needed only when running at work) | |
| R2 | Bitbucket MCP server name as shown in the tools picker, and the exact tool names for: create pull request; read branch / read repository (to re-confirm the remote SHA before creating); read existing pull requests for a branch (the required existing-PR check before creating)? Needed to replace the commented-out capability lines in `pr.agent.md`'s `tools:` list with live entries. | Sonnet | Open | |
| P3-C1 | Contract §4 says RUN.md records authorized exceptions, T13 prerequisite commits, and `TEST_CHANGE_REQUESTED` decisions "under the existing decision-recording convention", but RUN.md's fixed heading list (pipeline.agent.md, AGENT-CONTRACTS.md) had no existing heading for non-gate developer decisions before this checkpoint — earlier STOP decisions (`TESTS_NOT_RED`, `PRESERVATION_BASELINE_FAILED`, etc.) are instead recorded in the deciding subagent's own artifact, not in RUN.md. I added a new **Decision log** heading to RUN.md (owned by `pipeline`) to hold these, on the most conservative reading that §4 intends a durable, pipeline-owned record the Verifier can consult for "current" authorized exceptions. If a different placement (e.g. folding these into the existing Stage status table or Gates log instead of a new heading) was intended, please redirect. | Sonnet | Answered (Claude, P3-C1 review, 2026-09-10) | Accepted: a pipeline-owned **Decision log** heading in RUN.md is the intended "existing decision-recording convention" applied to non-gate decisions — same fields as the gates log (statement, answer, basis, status). Authorized exceptions must be durable, pipeline-owned, and readable by the Verifier for the current `NOT_VERIFIED` set, which a subagent-owned artifact cannot provide. Folding them into the Stage status table or Gates log was considered and rejected: the former has no answer/basis fields, the latter is reserved for G1–G4. Recorded as an accepted contract clarification at Phase 3 closure. |

## Current corporate validation checklist

Phase 5's current manual checks, open identifiers, result-field questions, and evidence slots are maintained in [`CORPORATE-ADOPTION.md`](CORPORATE-ADOPTION.md). The historical question rows above remain unchanged.
