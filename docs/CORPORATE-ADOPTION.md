# Corporate adoption checklist

This checklist validates the reference assets against the organization's actual GitHub Copilot, Jira, Bitbucket, Git, repository, and execution environment. It is an adoption record, not a new runtime or architecture. This Phase 5 checkpoint performed none of the checks and made no corporate mutation: every result starts **NOT VERIFIED**, and every evidence field is blank.

Run later checks only in an owner-approved disposable workspace, disposable repositories, non-production Jira data, and a non-production Bitbucket project. A potentially destructive or broader-than-approved terminal action is inspected in the approval preview and cancelled, never executed. Record the exact host, extension/version, date, fixture, and operator with each result.

Status values are **PASS** (the manual steps were observed and the expected result held), **FAIL** (an observed result contradicted the expectation), and **NOT VERIFIED** (not run, inaccessible, inconclusive, or missing evidence). A fallback may permit a narrower trial, but it never changes a failed or unverified automated capability into a PASS.

## Validation matrix

| ID | Validation and manual steps | Expected result | Result (`PASS`/`FAIL`/`NOT VERIFIED`) | Evidence and recorder | Failure scope | Honest fallback | Dependent reference behavior |
|---|---|---|---|---|---|---|---|
| CA-01 | In a disposable single-root workspace, open the Copilot custom-agent picker and record the displayed identifiers. Confirm `pipeline` is selectable. Then choose an actually available ordinary or other user-invocable chat role, invoke `/pipeline TEST-1`, and observe the requested-role guard without proceeding; do not assume any hidden stage agent is directly selectable. | `pipeline` is selectable and user-invocable. The eight stage agents may remain hidden because their frontmatter sets `user-invocable: false`; their configured inventory and routing are validated through CA-08. `/pipeline` outside `pipeline` asks for that role and performs no parsing, reads, or subagent work. | **NOT VERIFIED** | Evidence: ____; recorder/date/host: ____ | **Full adoption block:** entry and role isolation are unproven. | Invoke equivalent roles manually through the existing internal orchestrator and label the run a scoped adaptation, not a validated Copilot flow. | [`pipeline.agent.md` frontmatter](../.github/agents/pipeline.agent.md), [`pipeline` skill requested-role rule](../.github/skills/pipeline/SKILL.md) |
| CA-02 | With harmless fictional inputs, inspect tool/read traces for each role and invocation mode that names a policy skill. Confirm normal Tester construction, assessment, amendment, and correction invocations load `test-contract` in full; Tester activation mode follows its self-contained activation procedure and loads no policy or unrelated skill. Confirm Adversary loads the documented plan-review skills in plan-review mode and only the complete `test-contract` skill in test-review mode. Confirm the other roles load only their documented skills and non-entry skills do not appear as slash commands. | Policy reads match the documented mode-specific contracts, including activation's deliberate no-skill path; `disable-model-invocation` behavior matches the files. | **NOT VERIFIED** | Evidence: ____; mode/role trace: ____; recorder/date/host: ____ | **Scoped block:** any affected role/stage cannot be adopted; widespread load failure blocks full adoption. | Supply the same versioned policy through an approved internal skill mechanism and revalidate that route; pasting prose manually is not equivalent skill-loading validation. | [Asset-reading order](../README.md#how-to-read-the-assets), [skill/agent contracts](../.github/pipeline/AGENT-CONTRACTS.md), [Tester modes](../.github/agents/tester.agent.md#procedure) |
| CA-03 | Record the exact Sonnet-5 picker identifier, including vendor suffix. In harmless probes, select it and inspect available invocation metadata for `pipeline` and all eight subagents; repeat across a subagent boundary to detect fallback or inheritance drift. | The exact corporate identifier resolves to Sonnet-5 for all nine roles on every invocation. GPT-5.6 Sol authoring of these files is not evidence of corporate Sonnet-5 availability or routing. | **NOT VERIFIED** | Evidence: ____; exact model ID: ____; recorder/date/host: ____ | **Full adoption block:** the approved reference model assignment is unverified. | Manually select the recorded approved model before each scoped trial and record every invocation; this is an operating workaround until routing is validated, not proof of automatic routing. | [Model assignment and confirmation procedure](../.github/pipeline/MODEL-ROLES.md#first-implementation) |
| CA-04 | Compare each agent's actual granted tools in Copilot with its frontmatter and the least-privilege contract. Record exact host tool IDs. Confirm no wildcard MCP grant and no Jira write, PR merge/approve/decline/create-branch, terminal, or edit capability reaches a role that forbids it. | Actual grants equal the documented minimum. PR's only Phase 5 tool-list addition is `edit/editFiles`, confined to its two artifacts; only the documented four roles have terminal access; MCP tools are confined to Intake or PR. | **NOT VERIFIED** | Evidence: ____; grant export/screenshots: ____; recorder/date/host: ____ | **Scoped block:** disable the mismatched role; a grant system that cannot enforce role separation blocks full adoption. | Use the organization's existing per-role permission wrapper or run the affected stage manually with least privilege, then revalidate. | [Least-privilege table](../.github/pipeline/GUARDRAILS.md#least-privilege-table), [agent contracts](../.github/pipeline/AGENT-CONTRACTS.md) |
| CA-05 | In disposable artifacts, ask each role to update its owned artifact, then present an out-of-scope edit request and inspect the response/tool preview; cancel any preview outside the disposable owned path. Separately, from an ordinary authorized editing context in the disposable clone, request edits to protected instruction and settings paths under `.github/**` and `.vscode/**` only far enough to inspect the approval preview, then cancel without applying either edit. Record prose/path compliance and protected-path edit approval as separate observations. | Owned reads/writes/updates work and each role refuses other artifacts and repository paths. Protected instruction/settings edits are not silently approved. Generic edit tools plus prose ownership are **not** reported as a hard path sandbox. Absence of host path enforcement is recorded as an accepted boundary unless organization policy requires stronger enforcement. | **NOT VERIFIED** | Compliance evidence: ____; protected-edit preview evidence: ____; enforcement type: ____; recorder/date/host: ____ | **Scoped block:** observed role noncompliance blocks that role. Silent protected-file edit approval blocks full adoption. Missing hard enforcement blocks full adoption only if corporate policy requires it. | Require human edit approval and diff review, or use an existing corporate path-control layer. This mitigates risk but does not validate the reference flow's automated compliance. | [Artifact ownership](../.github/pipeline/GUARDRAILS.md#artifact-ownership-contract-82-verbatim), [self-protection](../.github/pipeline/GUARDRAILS.md#self-protection), [edit approval settings](../.vscode/settings.json) |
| CA-06 | In a disposable repository, observe one approved read-only Git command and one approved reversible branch-preparation command. Inspect the prompts for a test/build command. Enter representative destructive, force, and off-pattern commands only far enough to display the approval decision, then cancel them. | Exact allowlisted forms behave as configured; test/build commands prompt in Manual mode; destructive, force, chained, redirected, and off-pattern forms are denied or require an uncircumvented decision. No dangerous command is executed. | **NOT VERIFIED** | Evidence: ____; preview captures: ____; recorder/date/host: ____ | **Full publication/implementation block:** approval-engine behavior is unverified or bypassable. | Set terminal approval to manual for all commands and use human review. This supports a scoped trial but is not validation of the checked approval rules. | [VS Code approval settings](../.vscode/settings.json), [Git safety](../.github/pipeline/GUARDRAILS.md#git-safety) |
| CA-07 | Run a fictional, non-publishing path far enough to display G1-G3; inspect G4 with a prepared disposable PASS fixture and choose `ABORT`. Capture the actual question UI, choices, displayed basis, and RUN.md entry for every gate. | `vscode/askQuestions` displays the exact G1-G4 wording/options; only Pipeline asks; answers, basis, and `CURRENT`/`STALE` status are recorded; `ABORT` causes no publish or PR call. | **NOT VERIFIED** | Evidence: ____; RUN.md fixture: ____; recorder/date/host: ____ | **Full adoption block:** developer authority and publication consent cannot be demonstrated. | Conduct the same gate script through an existing audited human-approval channel and mark it a scoped adaptation pending Copilot prompt validation. | [Exact gates](../.github/pipeline/FLOW.md#gates), [`pipeline` gate contract](../.github/agents/pipeline.agent.md#gates-exact-wording) |
| CA-08 | In a disposable run, inspect `agent/runSubagent` calls for stage, round, run directory, inputs, and sole output. Trigger a safe documented STOP condition and verify Pipeline relays it verbatim and does not advance. Test one same-basis delivery continuation using already-recorded fixture state, with no external effect. | Pipeline invokes only the listed stage agent, preserves artifact ownership and mode-specific inputs, records history/decisions, relays the existing STOP text exactly, and resumes at the documented artifact rather than regenerating earlier work. | **NOT VERIFIED** | Evidence: ____; invocation/STOP trace: ____; recorder/date/host: ____ | **Full adoption block:** orchestration, STOP authority, and recovery are unproven. | Map the stages to the organization's existing orchestrator while preserving the same artifacts and gates, then validate that implementation separately. | [Stage sequence](../.github/pipeline/FLOW.md#stages), [resume rules](../.github/pipeline/FLOW.md#resume-by-artifact) |
| CA-09 | In the corporate tool picker/configuration, record the exact Jira MCP server ID and exact IDs for get-issue-with-field-names, list-comments, JQL search, and remote-link reads. Inspect request/response schemas and the role grant; do not add any write tool. | Every required read capability has one unambiguous exact ID and usable schema, is granted only to Intake, and no Jira write/edit/transition/comment/delete/link/attachment tool is granted. | **NOT VERIFIED** | Evidence: ____; server/tool IDs: ____; recorder/date/host: ____ | **Full stage-1 block:** requested Jira intake cannot run. Missing optional remote-link reads alone may be a scoped limitation if disclosed. | Use an owner-approved read-only Jira export to construct a manually labelled intake adaptation; it does not validate MCP retrieval. | [Jira capability expectations](../.github/pipeline/AGENT-CONTRACTS.md#mcp-capability-expectations), [`intake` MCP placeholders](../.github/agents/intake.agent.md#mcp-tools-to-enable) |
| CA-10 | Read an owner-approved non-production Jira issue whose fields and acceptance field are known. Record the exact response field names/types for key, summary, description, status, type, priority, components, labels, assignee, fix versions, parent, subtasks, links, and field-name/value metadata. | The response supplies the documented fields and lets Intake select the acceptance-criteria custom field by a case-insensitive field **name**, without assuming an instance-specific field ID; missing acceptance data becomes an Unknown. | **NOT VERIFIED** | Evidence: ____; response-field map: ____; recorder/date/host: ____ | **Full stage-1 block** if core fields or names cannot be read; **scoped block** for a ticket whose required field is missing. | Provide an approved immutable export with names and values and record the gap; do not claim connector validation. | [Requested Jira fields](../.github/skills/gather-jira-context/SKILL.md#fields-retrieved-per-requested-key), [INTAKE template](../.github/pipeline/AGENT-CONTRACTS.md#intakemd-owner-intake) |
| CA-11 | Against non-production issues with more than 20 requested-issue comments and more than 5 TESTING-issue comments, document pagination request/response fields, page-size behavior, continuation tokens, ordering, total-count meaning, and how completeness is known. Follow pages only to the documented bounds. | Intake can obtain latest-first bounded results, records total/partial status, stops at 20 and 5 respectively, and discloses truncation. An empty page, missing token, or maximum page size is not silently treated as complete without an authoritative completeness signal. | **NOT VERIFIED** | Evidence: ____; pagination/result fields: ____; recorder/date/host: ____ | **Scoped stage-1 block** for tickets whose required context cannot be bounded or proven complete; systematic ambiguity blocks full adoption. | Supply a pre-bounded, ordered export and retain a `NOT VERIFIED` connector result; the export is a scoped adaptation. | [Jira bounds](../.github/skills/gather-jira-context/SKILL.md#bounds-constants), [disclosure rules](../.github/skills/gather-jira-context/SKILL.md#disclosure) |
| CA-12 | Read a non-production Jira fixture with parent, subtask, TESTING, dependency, ordinary, and remote links. Record actual direction/type/key fields and JQL results when the issue payload omits a traversable relation. Confirm depth 1, deduplication, `MAX_RELATED = 15`, reduced related fields, and read-only behavior; perform no Jira mutation. | Relationship classes can be derived from authoritative fields; JQL is used only for omitted links/subtasks; remote links remain optional context; no prose-mentioned key is fetched; missing/limited data is disclosed and no Jira write occurs. | **NOT VERIFIED** | Evidence: ____; link/JQL field map: ____; recorder/date/host: ____ | **Full stage-1 block** for unreliable core traversal or any write; **scoped limitation** for optional remote links when honestly omitted. | Provide an approved relationship manifest or omit optional context with a warning; neither validates live Jira traversal. | [Traversal and relationship classes](../.github/skills/gather-jira-context/SKILL.md#traversal), [Intake failure behavior](../.github/skills/gather-jira-context/SKILL.md#failure-behavior) |
| CA-13 | Record the exact Bitbucket MCP server ID and the actual tool ID or IDs that collectively provide create PR, read repository/branch, list PR candidates, and direct read by known PR identity. Do not assume one tool per capability. On owner-approved non-production data, map clone URL/repository ID, source and target repository IDs, source and target branch names, branch SHA, PR ID/state/body/URL, errors, and timestamps returned by tools. | The three documented capability groups map to whatever number of actual connector tools supplies their required authoritative fields and resolves one G4 destination plus the exact source/target tuple. There is no fixed tool-ID count, no invented capability, and no merge/approve/decline/create-branch grant. | **NOT VERIFIED** | Evidence: ____; server/tool/result-field map: ____; recorder/date/host: ____ | **Full stage-10 block:** automated create/reuse cannot run. Earlier stages and approved `PUBLISH_ONLY` may be evaluated separately. | Stop at `PUBLISH_ONLY` and hand links to a human PR process; that is not automated stage-10 validation. | [Bitbucket capability expectations](../.github/pipeline/AGENT-CONTRACTS.md#mcp-capability-expectations), [`pr` MCP placeholders](../.github/agents/pr.agent.md#mcp-tools-to-enable) |
| CA-14 | In a non-production repository with multiple OPEN and CLOSED PRs for the same source branch, record list pagination fields and retrieve every page or an authoritative complete result. Then read a known CLOSED PR directly by its recorded identity even if branch lookup excludes it. | Completeness is demonstrable; candidate records include both repository identities, both branches, state, current source SHA, body, and URL; a known PR remains directly readable and cannot be mistaken for no history. | **NOT VERIFIED** | Evidence: ____; pagination/direct-read trace: ____; recorder/date/host: ____ | **Full stage-10 block:** incomplete lookup can cause unsafe reuse or duplication. | A human performs authoritative candidate and known-identity reconciliation while PR.md stays `BLOCKED`; do not create automatically. | [PR recovery contract](../.github/pipeline/AGENT-CONTRACTS.md#pr), [`pr` reconciliation procedure](../.github/agents/pr.agent.md#procedure) |
| CA-15 | Only after CA-13/14 pass, use an owner-approved disposable Bitbucket project to create one non-production PR. Verify PR.md intent exists before create; capture the create response, then mandatorily re-read the PR identity/state/body and remote branch SHA. Separately use an approved connector failure fixture, if available, to observe an unknown result without authorizing another create; otherwise leave that part NOT VERIFIED. | A conclusive create yields a stable identity/URL and matching OPEN PR plus verified SHA on reread. Timeout/inconclusive response records `BLOCKED` unknown; empty/inconclusive lookup never permits retry; an authoritative exact match may recover, otherwise human resolution and explicit retry authorization are required. | **NOT VERIFIED** | Evidence: ____; disposable PR/trace: ____; recorder/date/host: ____ | **Full stage-10 block** on missing post-read, ambiguous result, duplicate risk, or mismatched identity/SHA/body. | Use `PUBLISH_ONLY` and a human-created PR, retaining the automated result as unverified or failed. | [PR stage procedure](../.github/agents/pr.agent.md#procedure), [delivery recovery guardrail](../.github/pipeline/GUARDRAILS.md#delivery-recovery-and-pr-finalization) |
| CA-16 | Open an owner-approved parent folder as one VS Code root with two sibling disposable repositories whose directory names satisfy the literal bare-directory rule. Confirm both appear in inventory, searches remain repository-scoped, `git.autoRepositoryDetection = subFolders` works, and terminal forms address each exact directory. Also record the behavior for spaces, nesting, symlinks, and multi-root workspaces without forcing them through the reference flow. | Supported sibling repositories are independently visible and use one shared feature-branch name in RUN order. Unsupported directory shapes are rejected or documented, never silently quoted, flattened, or addressed through a different repository. | **NOT VERIFIED** | Evidence: ____; layout/visibility trace: ____; recorder/date/host: ____ | **Scoped block:** affected layouts and multi-repository runs cannot be adopted; single supported repositories may be evaluated separately. | Create an approved compatible disposable root or run one repository at a time with human cross-repository coordination. That does not validate multi-repository atomicity. | [VS Code prerequisites](../README.md#trying-it-in-vs-code-prerequisites), [Workspace command forms](../.github/agents/workspace.agent.md#procedure) |
| CA-17 | Before Pipeline runs, a human uses the organization's approved Git configuration inspection method **outside** the pipeline allowlist. Inspect effective fetch and push endpoints, URL rewrites/redirects, `pushInsteadOf`, mirror/refspec behavior, implicit follow-tags or tag-push behavior, and system/global/local hooks for each disposable repository. Record the resolved read/write repository identity and all possible side effects. | Fetch/read and push endpoints unambiguously identify the G4 repository; an explicit SHA-to-branch push cannot update extra refs/tags or trigger unapproved local effects. Configuration whose effects exceed the approved repository/ref/SHA tuple is corrected before use. No new pipeline command or setting is added. | **NOT VERIFIED** | Evidence: ____; configuration review: ____; recorder/date/host: ____ | **Full stage-9 block** for every affected repository; stages before publication may still be evaluated. | Use the organization's existing reviewed publication mechanism and treat delivery as a scoped adaptation, or stop before G4. Command-form approval alone is never cited as a sandbox. | [Host-environment assumptions](../.github/pipeline/GUARDRAILS.md#host-environment-assumptions), [verified publish procedure](../.github/agents/workspace.agent.md#procedure) |
| CA-18 | In the actual corporate container/session type, use a disposable repository to resolve the repository's declared packages and launch its real contract and full-suite commands under Manual approval. Record filesystem, credentials, proxy, DNS, certificate, registry, MCP endpoint, subprocess, resource, and timeout behavior. | Required tools and packages resolve through approved sources; the runner launches and returns complete, attributable output without bypassing corporate controls. | **NOT VERIFIED** | Evidence: ____; environment/runner trace: ____; recorder/date/host: ____ | **Scoped block** for affected repositories/commands; a platform-wide runner failure blocks implementation and verification adoption. | Run through an existing approved corporate CI/runner and attach its evidence as an adaptation. If meaningful evidence remains unavailable, use `RUNNER_UNAVAILABLE`; do not record RED, GREEN, or PASS. | [Environment failure classification](../.github/skills/implementation-quality/SKILL.md#iq14--could-not-verify--failed--passed), [host assumptions](../.github/pipeline/GUARDRAILS.md#host-environment-assumptions) |
| CA-19 | For representative disposable clauses that require a database and Kafka, run the real proof boundary with approved non-production services. Record service versions, network/auth access, topic/schema/database setup, isolation/reset, deterministic fixtures, cleanup ownership, and whether the responsible component rather than a proxy was exercised. | The contract and full suite obtain repeatable business-outcome evidence. Missing infrastructure, partial evidence, or a mocked proxy is labelled accurately and never promoted to PASS. | **NOT VERIFIED** | Evidence: ____; DB/Kafka proof trace: ____; recorder/date/host: ____ | **Scoped block** for the affected clauses/repositories. A current `NOT_VERIFIED` set yields `INCOMPLETE` and blocks G4, while unrelated supported work may be evaluated separately. | Seek equivalent evidence under the existing T10 policy or record an authorized gap. Partial/manual evidence remains a scoped adaptation and cannot validate the automated proof path. | [Coverage-gap policy](../.github/skills/test-contract/SKILL.md#t10--coverage-gaps-equivalent-evidence-partial-evidence-not_verified-incomplete), [verification verdicts](../.github/skills/implementation-quality/SKILL.md#iq14--could-not-verify--failed--passed) |
| CA-20 | After prerequisite rows pass, run one owner-approved end-to-end non-production scenario through stages 0-10 using disposable Jira data and repositories. Observe actual terminal and edit operations, G1-G4, subagents, all-repository publication, PR intent/create/post-reads, artifacts, STOP relay, and final completion accounting. Use an existing internal test harness if one is already available; do not build a new runtime for this check. | The observed run matches the documented stage order and ownership, carries current PASS/G4/SHA evidence through recovery, creates no duplicate, and completes only when every selected publication and required PR is confirmed. | **NOT VERIFIED** | Evidence: ____; run/artifact links: ____; recorder/date/host: ____ | **Full adoption block:** the assembled corporate flow remains unvalidated even if component checks pass. | Adopt only the stages whose prerequisite rows passed, with explicit human handoffs and limitations. A partial rollout is not an end-to-end PASS. | [Current flow](../.github/pipeline/FLOW.md), [completion contract](../.github/agents/pipeline.agent.md#procedure) |
| CA-21 | In the owner-approved disposable corporate host, use existing telemetry/export facilities to capture one metadata-only run with a subagent boundary, retry/resume, tool failure, and human wait. Record actual run/session/stage invocation relationships, source/completeness labels, leaf usage fields, unavailable fields, and content-capture behavior; inspect and redact any export before sharing. | Ordered correlation survives session and subagent boundaries; pending/unknown states remain explicit; leaf usage is counted once; a tool failure and uninstrumented wait are not misclassified; no content is captured beyond corporate policy. | **NOT VERIFIED** | Evidence: ____; private export/trace receipt: ____; recorder/date/host: ____ | **Scoped audit block:** performance or model comparisons cannot use incomplete or mis-correlated run data. | Use approved artifact and Git evidence only and label telemetry measures unavailable; do not infer missing values or add a collector here. | [Run correlation and capture guidance](RUN-AUDIT-AND-IMPROVEMENT.md#correlation-and-minimum-record) |
| CA-22 | Using existing private archive/versioning in an owner-approved disposable corporate environment, capture complete artifact bytes before an overwrite and at a gate/STOP; record receipts, then restore a declared complete checkpoint in an independent workspace. Exercise ownership transfer and one pending external effect without repeating it. | Exact prior bytes are retrievable; checkpoint completeness and the last durable state are explicit; the new owner starts from live repository/remote reconciliation; missing snapshots remain audit gaps; archived approvals do not authorize new effects. | **NOT VERIFIED** | Evidence: ____; snapshot/restore receipts: ____; recorder/date/host: ____ | **Full recovery-audit block:** do not claim durable auditability or coherent restoration. | Preserve current artifacts and Git evidence, stop replacement when required evidence cannot be archived, and reconcile effects manually under existing controls; do not give agents archive-write authority. | [Snapshot and multi-developer guidance](RUN-AUDIT-AND-IMPROVEMENT.md#snapshot-before-replacement) |
| CA-23 | In the corporate VS Code Local session and Copilot plan, record whether Copilot Memory is present, whether it is enabled by default, and whether organization or enterprise administrators control it; whether repository-level facts are stored and where they are visible (repository settings for Copilot memories, per the public documentation); whether stored facts are applied in Copilot code review; whether the documented surfaces (cloud agent, code review, CLI) include the Local session at all. | Exact observed availability and policy recorded; nothing assumed from public documentation. | **NOT VERIFIED** | Evidence: ____; policy/settings capture: ____; recorder/date/host: ____ | **Adoption limit:** if enabled and unmanaged, a parallel repository memory exists outside version control and outside this reference's controls. | Follow organization policy to disable or govern it; do not rely on it and do not describe it as part of this reference. | Draft record, no authority: [investigation §3.1, Vendor designs (Copilot Memory)](specs/2026-09-14-persistent-knowledge-layer-architecture-investigation.md#31-vendor-designs-that-constrain-or-validate-the-approach), [§19 Security and governance](specs/2026-09-14-persistent-knowledge-layer-architecture-investigation.md#19-security-and-governance) |
| CA-24 | In an owner-approved disposable workspace under Manual permission mode, record observations in two groups. **Retrieval prerequisites** (needed before a Planner-only trial with a hand-written map): (a) with a frozen `docs/knowledge/` tree at a recorded commit, invoke the Planner in a harmless fictional run and establish which knowledge files it actually read from **independently observed read evidence** — retained host tool calls and results (the CA-21 export or the chat debug view) compared against the frozen snapshot; the artifact `inputs:` line and evidence rows may corroborate that evidence but never substitute for it; (b) by the same evidence, observe that a stated but unconfirmed snapshot identity leads the Planner to ordinary discovery rather than to a newer entry. Where host visibility cannot establish which snapshot was used or whether the fallback was taken, the retrieval prerequisites stay `NOT VERIFIED`. **Assisted-curation prerequisites** (needed only if a curator agent or prompt file is later proposed): (c) with a `chat.tools.edits.autoApprove` glob set to `false` for a documentation directory, observe whether create, edit, multi-file edit, and a rejected edit each prompt before the change is applied; (d) observe that selecting a custom agent, or running a prompt file with a `tools` list, yields the intended effective tool set, and that invoking a skill alone does not change the selected agent or its tools; (e) observe that an agent absent from `pipeline`'s `agents:` list cannot be reached through `agent/runSubagent` from `pipeline`; (f) observe and record honestly whether a general edit tool changes a file outside the glob without a prompt. | Each behaviour recorded as observed, by group, from independently observed read evidence corroborated by artifact declarations; none is described as a sandbox or path enforcement. | **NOT VERIFIED** | Evidence: ____; prompt/approval trace: ____; recorder/date/host: ____ | **Trial block, by group:** retrieval prerequisites unmet → no Planner-only retrieval trial; assisted-curation prerequisites unmet → no curator agent or prompt file. The observations are prerequisites, not permission; each trial needs its own separate authorization. | Retrieval group unmet: no runtime retrieval. Curation group unmet: human-only curation. | Draft record, no authority: [investigation §18, Agent responsibilities](specs/2026-09-14-persistent-knowledge-layer-architecture-investigation.md#18-agent-responsibilities), [§22, Smallest useful experiment](specs/2026-09-14-persistent-knowledge-layer-architecture-investigation.md#22-smallest-useful-experiment), [§23, Incremental rollout plan (K2, K3)](specs/2026-09-14-persistent-knowledge-layer-architecture-investigation.md#23-incremental-rollout-plan). The trial itself is not authorized. |

## Portability boundary

| Asset or behavior | Classification | Adoption treatment |
|---|---|---|
| Stage order, artifact ownership, provenance, gates, STOP semantics, bounded loops, current-basis recovery, exact-SHA publication, and PR reconciliation | Portable policy | Copy or map into the existing internal orchestrator while preserving the stated invariants; validate the mapped behavior with this checklist. |
| `.agent.md` frontmatter, `.github/skills/**` packaging, `agent/runSubagent`, `vscode/askQuestions`, model picker behavior, and `.vscode/settings.json` approval regexes | GitHub Copilot-specific packaging | Validate in the actual corporate Copilot host. An internal equivalent is an adaptation and needs its own evidence. |
| Jira/Bitbucket server IDs, tool schemas and pagination, repository layout, Git configuration, containers, networks, packages, databases, and Kafka | Corporate host-specific dependency | Record exact observed values and limits. Do not fill placeholders from public examples or this authoring environment. |
| Manual exports, human PR creation, human publication, manually selected models, or external CI evidence | Scoped fallback | Use only with an explicit boundary and handoff. Never present it as equivalent validation of the automated corporate flow. |

## Common practice and team-owned adaptation

The goal for other teams is consistent decisions and evidence, with team-specific execution. Nine roles, twelve filenames, Jira keys, the current budget values, and one model assignment are this reference workflow's own choices, not the part meant to be copied literally.

| Common practice | Team-owned adaptation |
|---|---|
| Explicit goal, permitted scope, exclusions, material decisions, completion evidence and escalation | Work-item source, terminology, repository structure and domain authority |
| Source-linked requirements; assumptions distinguished from facts and approved decisions | Existing product specifications, architecture records, service contracts and responsible owners |
| Least necessary capabilities; external actions authorized for their actual target and effect | Corporate tools, runner/CI, credentials, approval engine and deployment controls |
| Evidence that observes the required outcome; independent challenge where it adds value | Test frameworks, persistence/transport/UI proof facilities and existing review process |
| Current evidence and approval bases; known state, remaining allowance and honest recovery | Existing host sessions, artifact storage, checkpoints and operational handoff mechanisms |
| Reviewed, versioned improvement with measured results | Private telemetry backend, retention policy and team adoption/release process |

A team adopting this reference records a short **team setup record**, as a checklist, not a new machine-readable profile, dispatcher, or central policy service:

- authority/domain entry points;
- allowed repositories and tools;
- proof routes and prerequisites;
- approval owners and effect boundaries;
- private evidence/storage policy;
- adopted reference version (tag or commit) and any documented deviations.

Use the team's existing corporate configuration and documents to fill this in. This is onboarding guidance, not infrastructure this repository runs or hosts.

## Team context and proof-route entries

For an adopted target repository, a team records a compact entry per domain question it needs to answer repeatedly: **domain question · authoritative source and revision · component owner · proof route (existing test, command, or inspection) · prerequisites · known limits of the resulting evidence.**

Real entries live in the team's own approved location — never in this reference repository. A map only locates evidence; Planner still has to inspect it, resolve contradictions, and disclose uncertainty (`plan-grounding/SKILL.md` R3, repository evidence; R5, Observed at). Two one-line examples of the discrimination a proof-route entry should preserve, not resolve for every case: a validator's local return value is not the same evidence as the actual endpoint's status, serialized field name, and error body; an intended save call is not the same evidence as observed mapping/storage behavior. Only the entry shape belongs in this reference: no business facts from its fictional examples and no corporate names.

## Proportionate use across daily work

| Work | Appropriate use of the reference |
|---|---|
| Research, incident investigation, design analysis or code review | Scope, source evidence, uncertainty, useful findings and a clear handoff. No invented RED/GREEN cycle or publication gate for a read-only assignment. |
| Documentation-only change | Verify the actual documentation outcome and links as authorized; do not manufacture executable business tests merely to populate artifacts. |
| Small behavior change or bug fix | Concise requirements and material decisions, meaningful business proof, bounded implementation and independent verification/review appropriate to the adopted workflow. Small size does not excuse weak evidence. |
| Cross-component or cross-repository change | Explicit interfaces, compatibility assumptions, ownership, order where material, and evidence on both sides of changed boundaries. |
| Release, merge, deployment or production operation | Use the organization's existing authorized delivery procedure. This reference's push/PR procedure does not implement merge or deployment. |

This table guides selective corporate adaptation. It does not authorize a current `/pipeline` run to skip its prescribed stages, exempt SMALL from existing review, or change G3/G4.

## Staged adoption using existing infrastructure

1. Complete CA-01 through CA-06 in a disposable local workspace; do not enable connector or publication effects yet.
2. Complete CA-07 and CA-08 with fictional/local artifacts, ending at `ABORT` or a documented STOP.
3. Complete CA-09 through CA-12 with read-only non-production Jira access.
4. Complete CA-16 through CA-19 for each repository class and execution environment before allowing its implementation, verification, or publication stages.
5. Complete CA-13 and CA-14 read-only, then CA-15 in an owner-approved disposable Bitbucket project.
6. Run CA-20 only after its dependencies pass. Record remaining FAIL or NOT VERIFIED items as explicit adoption limits rather than adding a runtime, schema, deployment, or new integration to this repository.
7. Complete CA-21 and CA-22 before claiming correlated performance evidence, durable artifact history, or restorable multi-developer state.

The checklist records adoption evidence only. Jira transitions/comments and production PR, publish, rollout, database, or Kafka mutations remain outside this Phase 5 checkpoint and under the organization's existing controls.

CA-23 and CA-24 are observation rows for a possible later knowledge-map trial; they may be run alongside steps 1–2, and they authorize nothing.
